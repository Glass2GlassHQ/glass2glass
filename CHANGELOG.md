# Changelog

Pre-release. Work is tracked by milestone (Mn) following the roadmap in `DESIGN.md` §4.10.
Versioning and the stability tiers are defined in `STABILITY.md`; `g2g-core` is the
semver-covered surface, the plugin/binding crates are provisional or experimental.

## Unreleased

- M779: AV1 demux: the MP4 demuxers read the `av01` sample entry (av1C config, pass-through OBU samples), so M773's muxes round-trip byte-identical both ways and ffmpeg-muxed AV1 MP4s demux.

- M778: Ogg granule timing: Vorbis packets get sample-accurate pts/durations from the setup header's mode tables (ffmpeg's backward-scan technique), anchored to the first page's granule and clamped to the end granule, so decode length matches ffmpeg exactly.

- M777: Vorbis decode: `oggdemux stream=vorbis` + the pure-Rust `vorbisdec` (symphonia, `vorbis` feature), auto-plugged by `decodebin`/`playbin`; PCM within 1 LSB of ffmpeg's decode.

- M776: a mid-stream caps re-solve that defers no longer misfeeds the element's input caps to `configure_output`; Ogg-Opus files now play through `run_graph` / `playbin`.

- M775: Ogg-FLAC: `oggdemux stream=flac` demuxes the `\x7fFLAC` mapping (bit-exact decode vs ffmpeg), `decodebin`/`playbin` sniff the codec and auto-insert `flacparse`, and lone audio files (`.flac` / `.oga` / `.opus`) play via a `playbin` hook.

- M774: native `.flac` support: `filesrc` types the file (extension + `fLaC` sniff) and the new `flacparse` splits CRC-validated frames with STREAMINFO caps, framing oracled against ffprobe.

- M773: AV1 in the fan-in muxers: `mkvmuxn` writes `V_AV1` + av1C `CodecPrivate` and `mp4mux` an `av01` sample entry, the record byte-identical to ffmpeg's for the same stream.

- M772: `G2G_DEBUG` category names take `*` / `?` globs (`*sink*:5`); an exact override wins over a glob.

- M771: `OpusEnc` accepts F32 PCM input and `OpusDec` emits F32 when negotiated, via libopus' float APIs.

- M770: `matroskamux` / `mkvmuxn` gain a `seekable` two-pass mode that finalizes the file with a front `SeekHead`, so it seeks from byte 0.

- M769: `FfmpegAacEnc` end-to-end encode test (ADTS framing checked field-by-field, tone decoded back by ffmpeg and verified at 440 Hz).

- M768: the FLV muxers stamp tags with DTS and write the signed composition-time offset (`pts - dts`), so B-frame presentation times survive the mux.

- M767: the MP4 demuxers read the `Opus` sample entry (`dOps`), so an Opus track surfaces as `Caps::Audio { Opus }` with raw packets, validated against an ffmpeg-authored file.

- M766: the Matroska demuxers convert AVCC / HVCC H.264 / H.265 tracks to Annex-B (parameter sets prepended on keyframes), so `decodebin` on an `.mkv` NAL-splits cleanly.

- M765: the IMA ADPCM decoder reconstructs with the spec's bit-serial arithmetic (matching ffmpeg >= 8.1); the m639 oracle classifies a legacy ffmpeg by output and skips its decode-exactness asserts.

- M764: `VulkanVideoDec` rebuilds the session on a same-geometry parameter-set content change and sizes its reorder buffer from the stream's declared depth (H.264 VUI / H.265 SPS) instead of the DPB size.

- M763: sites that hand-rolled a `MemoryDomain::System` match to read CPU bytes now call `MemoryDomain::as_system_slice()`.

- M762: the browser runs a real ONNX model over decoded video via onnxruntime-web (`WebSocketSrc -> WebCodecsDecode -> WebOrtDetect -> CanvasSink`, CPU tensors, plain static HTTPS, no COOP/COEP), validated headless against a committed model fixture.

- M761: `UdpSink`'s FEC modes are settable via `fec-mode` / `fec-columns` / `fec-rows` / `fec-payload-type` / `fec-ssrc` launch properties, not builders only.

- M760: an `offload` feature lets a heavy synchronous element move its per-frame compute onto tokio's blocking pool, so the cooperative runner keeps rendering sibling arms without opting into thread-per-arm (`videoconvert`, `waylandsink`, `ffmpegdec`).

- M759: per-frame metadata survives a linear transform, not just a tee: an element declares a `meta_transform` and the runner propagates and re-attaches the surviving meta to its fresh outputs.

- the `revideo` streaming-adapter module is renamed `streamdec` (API rename, no behavior change).

- M758: FlexFEC gains 2-D (row + column) protection, so a burst of up to a full row of consecutive losses recovers via the column repairs.
- M756: device-validated that `MediaCodecDec` accepts and forwards the runner's pre-fixed output `CapsChanged`, with on-device regression coverage in the decode smoke test.
- M755: `audioresample ! audioconvert ! rate-pin` now flows like the reverse order; the resampler keeps its caps-resolved target across an intervening converter.
- M754: audio decoders negotiate a downstream pin at the stream's native sample rate with no resampler; a cross-rate pin without one still fails loud.
- M757: AC-3 decodes from MPEG-TS (ATSC and DVB carriage) and Matroska, and FLAC from Matroska (`CodecPrivate` forwarded as decoder extradata).
- M753: MPEG-TS demux carries MPEG audio (`mp2`) and Opus elementary streams, selectable and auto-plugged like AAC.
- M752: `audioresample` flushes its interpolation carry at end of stream when resampling, so the output reaches the rate-ratio-consistent sample count instead of stopping one window short.
- M751: `audioresample` at rate 1:1 is a byte-exact pass-through instead of losing the stream's final sample to the interpolation carry.
- M750: Opus decode discards the `OpusHead` pre-skip and the Ogg granule-position end padding, so decoded PCM has the same sample count as ffmpeg / gstreamer.
- M749: a linear decode chain whose infeasible mid-stream caps refinement cannot cross a downstream pin (e.g. 44.1 kHz decode reaching a `rate=48000` capsfilter with no `audioresample`) now fails loud naming the conflict instead of writing native-rate samples under the pinned label.
- M748: a bare `filesrc ! decodebin` on an audio-only MP4 sniffs the `moov` and selects the demux's audio track, so it plugs an audio decoder instead of failing on the default video port.
- M747: a `decodebin` fan-out audio branch survives a runtime sample-rate refinement (an `audioresample` bridges 44.1 kHz decode to a 48 kHz pin), and without a bridging converter the branch fails loud naming the conflicting caps instead of silently or opaquely.
- M746: a bare `filesrc ! decodebin` on an audio-only MPEG-TS sniffs the PMT and selects the demux's audio stream, so it plugs an audio decoder instead of failing on the default video port.
- M745: decode-to-PCM launch lines negotiate: audio decoders fixate a concrete PCM output, `audioconvert` is caps-driven (format/channels), and `wavsink` is registered.
- M744: `VulkanVideoDec` streams display order on the GPU-texture path (H.264/H.265 texture reorder) and on AV1 (per-AU op-walk incl. `show_existing_frame` + film grain); fixes an AV1 session use-after-free (the driver retains the Std sequence header).
- M743: Vulkan Video H.265 long-term reference pictures, bit-exact on the LTRPSPS conformance vector; also fixes slice-inline inter-predicted RPS parsing (`delta_idx_minus1`, `NumDeltaPocsOfRefRpsIdx`).
- M742: Android on-screen present harness (`examples/g2g-android-present` + `tools/android-apk-present-smoke.sh`): a NativeActivity APK decoding zero-copy to the GPU and presenting to the activity's real window, device-validated.
- M741: Bevy embedder example (`examples/bevy-g2g-decode`): a stock Bevy app adopts its own wgpu device via `GpuContext::from_wgpu` and samples g2g-decoded frames zero-copy on a 3D surface.
- M740: on-screen macOS present example (`metal_video_on_screen`): VideoToolbox zero-copy decode into an app-owned window's `CAMetalLayer` via `MetalVideoSink::with_layer`.
- M739: ScreenCaptureKit display capture (`screencapturesrc`, `screencapture` feature) with zero-copy `cv-output`; the CI runner denies screen recording, so the probe path is validated and real capture awaits a permitted Mac.
- M738: AVFoundation capture (`avfvideosrc` camera with zero-copy `cv-output`, `avfaudiosrc` mic, `avfoundation` feature); mic capture validated on the CI runner, the camera path probes the permission/no-device denial until a Mac with one.
- M737: Core Audio elements (`coreaudiosink` / `coreaudiosrc`, `coreaudio` feature) on the AudioToolbox AudioQueue API, with `autoaudiosink` / `osxaudiosink` aliases; render and capture validated on the CI runner's audio devices.
- M736: `MetalVideoSink` (`metalvideosink`, `metal-sink` feature): NV12 presented to a `CAMetalLayer` drawable, zero-copy from the CvPixelBuffer domain via IOSurface plane import; render + present validated on the macOS CI runner, `autovideosink` resolves to it on macOS.
- M735: `CVPixelBuffer` zero-copy memory domain (`MemoryDomain::CvPixelBuffer`): `VtDecode cv-output` emits retained IOSurface-backed buffers and `VtEncode` encodes them directly, transcode validated on the macOS CI runner.
- M734: the MF and MediaCodec codecs accept the graph runner's pre-fixed output caps and emit concrete output framerates (the M733 VideoToolbox fixes applied to the sibling platform codecs).
- M733: VideoToolbox elements in the launch registry (`vtdec` / `vtdech265` / `vtenc_h264` / `vtenc_h265`, `avdec_h264` alias fallback, encoder `bitrate` / `max-keyframe-interval` properties); text pipelines validated on the macOS CI runner.
- M732: migrate the VideoToolbox elements off the deprecated objc2 free functions to the associated-function forms.
- M731: `VtDecode` / `VtEncode` runtime-validated on the macOS CI runner (reference-fixture decode and encode round trip, H.264 and HEVC), with persisted hardware conformance evidence.
- M730: VP8/VP9 runtime force-keyframe and bitrate retarget (encoder rebuild on the reverse signals, hysteresis-gated).
- M729: duplex renegotiation (`DuplexControl::set_track_enabled` renegotiates the m-line direction mid-session; peers answer re-offers); pause/resume validated on a live P2P loopback.
- M728: `LiveKitDuplex` full participant (publisher + subscriber PCs over one signalling socket, on the duplex runner); two participants exchanging A/V validated live.
- M727: terminal fan-out source graph node (`Graph::add_fanout_src` + `FanoutSrcFactory` launch wiring), so session sources (`livekitsrc`, `webrtcwhepsessionsrc`) run inside graphs and launch lines; validated live against a LiveKit server.
- M726: mono Opus accepted on the WebRTC sinks and a graceful EOS pacer drain (queued RTP flushes before the socket drops).
- M725: `webrtcsessionsink` / `livekitsink` in the launch registry as terminal fan-in elements (track kinds from each linked pad's caps, endpoint/room via properties).
- M724: mid-stream caps re-solve through the fan-in session runners (a source's caps change re-configures its session input pad, standalone and graph arms).
- M723: simulcast on the WHIP session sink (`WebRtcSessionSink::with_simulcast`, shared `SimulcastPads` pad model with `LiveKitSink`); machinery unit-tested, live multi-rid WHIP ingest validation owed (no local server supports it).
- M722: per-layer simulcast bitrate targets (the allocator splits the BWE estimate across layers; `Bitrate(0)` idles a shed layer's encoder) and `FfmpegH264Enc` runtime retarget via reopen; shed + idle validated live through the encoder fan graph.
- M721: Opus runtime bitrate adaptation (`OpusEnc` retargets the live encoder from downstream BWE estimates).
- M720: keyframe-request / bitrate reverse signals relay past non-consuming transforms to the encoder (`handles_keyframe_requests` / `handles_bitrate_requests` element capability), in the linear and DAG runners.
- M719: multiple TURN servers per element (comma-separated list, GStreamer-style `turn://user:pass@host` credentials) and TURN on the duplex session; multi-allocation validated against coturn.
- M718: IPv6 STUN/TURN (v6 XOR address codec, RFC 6156 v6 relay allocation, v6-capable host-ip pick); relay round-trip validated against coturn over `::1`.
- M717: TURN-over-TCP / -TLS (`turn:...?transport=tcp` / `turns:` server forms via a datagram-to-stream bridge); validated against coturn on all three transports.
- M716: TURN channel binding (ChannelData framing once the per-peer bind lands) and 438 stale-nonce recovery; validated against a real coturn relay.
- M715: `wss://` (TLS) LiveKit signalling via tokio-tungstenite native-tls; ingest loopback validated through a TLS-terminating proxy.
- M714: `LiveKitSrc` room subscriber (answers the server-offered subscriber PC, video + audio outputs, PLI until first keyframe); validated live incl. a sink-to-src loopback on a real server.
- M713: terminal fan-in graph node (`Graph::add_fanin_sink`, session sinks in `run_graph`/`parse_launch` with per-input Eos flush and per-pad reverse-signal routing), enabling the live encoder fan graph (`tee -> videoscale -> ffmpegenc` per simulcast layer into `LiveKitSink`); validated live incl. a Chrome subscriber.
- M712: third simulcast layer (rids `f`/`h`/`q`) and per-rid `a=rid` max-width/max-height restrictions from each pad's fixated caps; all three layers validated live incl. Chrome LOW/MEDIUM/HIGH switching.
- M711: simulcast BWE layer allocator (aggregate estimate budgeted as whole-layer on/off, top layer sheds first with hysteresis, last layer never drops) plus a `max-send-bitrate` cap property; shed + fallback validated live in Chrome.
- M710: two-layer send simulcast on `LiveKitSink` (`with_simulcast`, shared `webrtc_simulcast` machinery: one m-line with rid-tagged layers, per-`(mid,rid)` keyframe routing); validated live incl. a Chrome subscriber switching layers.
- M709: patch str0m to send media packets unpadded (fork via `[patch.crates-io]`, pending upstream str0m#1014 / livekit#4689), fixing browser playback of a `LiveKitSink` stream; validated live in Chrome.
- M708: native WebRTC data-channel elements (`WebRtcDataSrc` / `WebRtcDataSink`) on str0m SCTP, P2P over the `SdpChannel` seam, mirroring the wasm `WebRtcSrc` byte-stream surface.
- M707: native LiveKit publish signaller (`LiveKitSink`, `webrtc-livekit` feature): hand-rolled JWT + protobuf signal codec over WebSocket, one str0m PeerConnection into a room, validated against livekit-server.
- M706: WHIP/WHEP trickle ICE (immediate offer POST, post-offer STUN/TURN candidates via `PATCH` sdpfrag with a numeric-mid retry for mediamtx), ICE restart on sustained disconnect, and `DELETE` teardown on session end.
- M705: NACK / RTX loss recovery proven for the native duplex WebRTC session over a lossy P2P relay, with a default-run localhost test asserting near-complete H.264 delivery under ~10% packet loss.
- M704: browser interop validated live (Chrome plays a g2g WHIP A/V publish via mediamtx WHEP, and a Chrome-encoded H264 canvas publish is ingested by `WebRtcWhepSrc`), with two harness tests and a `whip-publisher` example page.
- M703: raise the workspace MSRV to 1.85 and put the `webrtc` feature under CI (compile check plus the deterministic + localhost-only tests).
- M702: the dashboard's compressed edge card parses the packet header for the frame type (H.264 / H.265 IDR vs delta, VP8 key vs delta) and shows the packet size, no decoder.
- M701: the builder embeds the wasm caps solver as base64 and instantiates it from bytes, so the real solver runs in the static bundle and the self-contained artifact (not just `pnpm dev`).
- M700: `ffmpegdec` seeds libavcodec with the stream's parameter sets and SPS-derived reorder depth at open, so a stream that reorders around its first IDR no longer loses the opening GOP's leading pictures.
- M699: the visual builder validates links with g2g's real caps solver compiled to wasm (`g2g-validate-wasm`), showing negotiated caps and flagging failing links, falling back to the family heuristic when the blob is absent.
- M698: `g2g-launch --help` lists the rest of the dev tooling (live dashboard, DOT, inspect, visual builder, record/replay) so it is discoverable from the tool devs reach for first.
- M697: visual builder imports a gst-launch line or declarative JSON onto the canvas, warns on incompatible link caps, and emits a branched export that actually parses.
- M696: dashboard edge preview thumbnails NV12 / I420 raw video and MJPEG keyframes, and shows a codec card for other compressed edges instead of a hexdump.
- M695: `g2g-launch` prints the failing caps negotiation (the conflicting elements and the caps each wanted) by default, without `G2G_CAPS_TRACE`.
- M694: live telemetry covers the threaded runner (new `run_graph_threaded_observed`) and the muxer / demux fan nodes, so a branched or multicore pipeline reports per-element `process()` latency and fill too.
- M693: `filesrc` content-sniffs raw Annex-B H.264/H.265 elementary streams, so an unknown or mis-named extension (e.g. a `.jsv` conformance vector) types by content instead of the MPEG-TS default.
- M692: builder palette adds a dynamic group for `uridecodebin` / `decodebin` (gst-launch export only).
- M687: `decodebin` auto-plugs `ffmpegdec` as `output-format=auto`, so a 4:2:2 / 4:4:4 source decodes to its native chroma when a downstream caps pins it (the launch path, not just the element API); the decoder's source pad template advertises I422 / I444.
- M691: builder property panel shows every property with enum dropdowns, defaults, ranges, and descriptions.
- M690: live dashboard shows each edge's negotiated caps and gains zoom / pan / fit for large graphs.
- M689: visual pipeline builder rebuilt on React Flow (Vite + pnpm, `tools/builder/`): pan/zoom, either-direction linking, typed property panels, exports gst-launch / declarative JSON.
- M688: visual builder fixes: links to far nodes no longer clip (SVG sized to the canvas), ports connect from either drag direction, and edges attach at the true port center.
- M687: `g2g-launch --observe-host <addr>` binds the dashboard to a chosen address (default `127.0.0.1`; `0.0.0.0` serves all interfaces, with a no-auth warning).
- M686: `ffmpegdec output-format=auto` matches the source chroma automatically, resolving I420/I422/I444 per frame from the decoded pixel format.
- M685: `ffmpegdec` preserves 4:2:2 / 4:4:4 chroma (`output-format=i422|i444`) instead of forcing 4:2:0; a decoded 4:4:4 stream is now byte-identical to ffmpeg's own decode.
- M684: measured per-link transit (queue-residency) time via a send-time stamp on Block edges into transform/sink arms, feeding an `ElementLatency::transit` distribution and the dashboard's per-stage latency waterfall (wait + process).
- M683: dashboard edge tap: click an edge to stream a live content preview (RGBA/BGRA thumbnail, PCM waveform, or hexdump) over the WebSocket, via a per-edge `LinkInterceptor` slot on every link.
- M682: `negotiate_graph_explained` preserves the structured `NegotiationFailure`, so `validate` reports per-edge negotiated caps and names the conflicting link.
- M681: `g2g-mcp` MCP server (JSON-RPC over stdio) exposes list_elements / inspect / validate / launch for agent-driven dev, over shared `toolingjson` internals.
- M680: `recordsink` / `replaysrc` capture a pipeline's packet stream to a file and play it back byte-identically, for deterministic repro.
- M679: `cargo xtask new-element <name> --kind source|transform|sink` stamps a compiling element skeleton, a scaffold test, and the `lib.rs` module wiring.
- M678: `g2g-inspect --json` emits a machine-readable registry dump feeding a static visual pipeline builder (`tools/builder/`) that exports gst-launch lines and declarative JSON.
- M677: live pipeline dashboard: `g2g-launch --observe <port>` streams per-element telemetry + bus events over a WebSocket to a served page, backed by a runtime `Observer` tap (`run_graph_observed`).
- M676: `filesrc` types raw Annex-B elementary streams by extension (`.h264`/`.265` and variants), and name-based `decodebin` expansion now splices the M421 re-framing parser ahead of the decoder.
- M675: `splitmuxsink` gains a `muxer` property (`mp4`/`matroska`/`mpegts`) to pick the per-segment child container.
- M674: adds `equalizer-3bands`, `spectrum` (FFT analyzer), `clockoverlay`, and `splitmuxsink`; `equalizer-10bands`/`-nbands` alias to `equalizer-3bands`.
- M673: fills GStreamer element gaps: `audioamplify`, `audioecho`, `level`, `cutter`, `gamma`, `deinterlace`, `timeoverlay`, `concat`, `input-selector`/`output-selector`, `progressreport`, `multifilesink`/`multifilesrc`.
- M672: `RtspServerSink` serves TCP-interleaved transport (RFC 2326), framing RTP as `$`+channel+length on the control connection.
- M671: `SrtSink` retransmits rekey KM until a KMRSP arrives; `SrtSrc` answers each rekey KMREQ with a KMRSP.
- M670: `AacParse` handles LOAS/LATM framing (MPEG-TS/broadcast), bit-parsing `AudioMuxElement`/`StreamMuxConfig`/`AudioSpecificConfig`.
- M669: `OpusParse` reads the true channel count from an in-band `OpusHead` for multichannel streams instead of relying on the TOC.
- M668: `TextOverlay` truetype backend swaps `fontdue` for `ab_glyph`, so OpenType-CFF/CFF2 fonts rasterize.
- M667: `VideoTestSrc` zone plate is now a true sinusoidal chirp instead of binary square-wave rings.
- M666: `Compositor` mixes NV12/I420/I422/I444 directly via planar per-channel blending, no RGBA round-trip.
- M665: `VideoBalance` gains a `hue` property, rotating chroma about the luma axis; adds a `mathf` sin/cos module.
- M664: `AudioMixer` aligns inputs by PTS on a shared sample timeline instead of arrival order.
- M663: `H265Parse` reads framerate from the VUI `timing_info`; shares `H265ShortTermRps` parsing with the Vulkan decoder.
- M662: FLV codec-config side channel both directions; `FlvMux` now writes a real playable FLV instead of raw bytes.
- M661: heap-free YUYV->I420 convert for `g2g-mcu`, plus a drafted STM32H743 Ethernet/RTP harness (compiles for thumbv7em).
- M660: hardware H.264 encoder seam (`H264Encoder`/`HwH264Enc`) plus a C callback bridge for ESP32-P4.
- M659: `SpiDisplaySink::with_stripe(rows)` streams horizontal bands instead of a whole-frame blit, for large panels that can't hold a full framebuffer.
- M658: drafted ESP32-P4 (RISC-V) Tier-1 board harness; heap-free display pipeline now runs via board-agnostic `run_display_with`.
- M657: RISC-V footprint model handles register-materialized stack frames, closing the flagship audio graph's footprint report.
- M656: RISC-V added as a second ISA for the heap-free/panic-free proofs; noalloc-check + footprint tooling cover both ISAs.
- M655: certification artifacts: a machine-checked requirements traceability matrix, a safety manual, and a qualification-kit script.
- M654: adds a SHT3x I2C sensor driver (`Sht3xSrc`) and UART transport (`UartSink`/`UartSrc`) to `g2g-mcu`.
- M653: RX path on the MCU: wire-tolerant `RtpHeader::parse`, `RtpSrc` ingress, and a heap-free `JitterBuffer` reorder element.
- M652: runtime fault-recovery supervisor (`g2g_core::supervise`): `FaultPolicy`, `Recover` trait, watchdog integration.
- M651: interrupt/DMA capture concurrency: lock-free `SpscFrameRing` hand-off plus the consumer-side `SpscCaptureSrc`.
- M650: C seam ABI: `CFrameGrabber`/`CPacketSender` C-callback adapters plus a frame-at-a-time `step_source_sink` runner.
- M649: brought public docs current with the heap-free MCU wedge: README's embedded section and docs/index.html catch up through M623+.
- M648: generalized `g2g-mcugen` beyond audio to a video/display graph catalog, adding a `spidisplaysink` element and per-sink codegen.
- M647: packaged the heap-free build as a reusable Zephyr module (`examples/g2g-zephyr-module`), wired via west manifest or `ZEPHYR_EXTRA_MODULES`.
- M646: host graph compiler `g2g-mcugen` compiles a declarative audio graph (YAML/JSON) into a monomorphized static MCU pipeline.
- M645: per-frame timing/jitter report for the flagship audio graph, via SysTick + QEMU icount for reproducible numbers.
- M644: composed the flagship demo graph (capture->convert->resample->mix->encode->RTP) as one static pipeline, verified on every proof target.
- M643: RTP egress seam (`RtpHeader`, `PacketSender`, `RtpSink`); de-duped five hand-rolled RTP header writers into one.
- M642: static fan-in (`StaticFanIn2`) plus a Q15 mixer for the audio chain's mix stage.
- M641: fixed-point polyphase resampler (`g2g-mcu::resample`) for mono S16LE at 8/16/48 kHz.
- M640: hardware JPEG decoder peripheral seam (`JpegDecoder`/`HwJpegDec`) for `g2g-mcu`.
- M639: IMA ADPCM fixed-point codec in `g2g-mcu::adpcm`, bit-exact against ffmpeg in both directions.
- M638: G.711 (mu-law/A-law) fixed-point codec in `g2g-mcu::g711`, bit-exact against ffmpeg over the full domain.
- M637: Zephyr executor proof, the fourth leg of the executor matrix; `examples/g2g-zephyr` links `libg2g_noalloc.a` via a normal Zephyr CMake build, verified on QEMU Cortex-M3.
- M636: fixed-rank `TensorShape` (inline dims, no heap) brings `Caps::Tensor` into the no-alloc MCU subset like every other caps kind.
- M635: dissolved the `g2g-enterprise` crate; `TensorBatcher` moved to `g2g-ml::batcher`, its CUDA<->wgpu e2e tests to `g2g-ml/tests/`.
- M634: `forbid(unsafe)` application posture proven: a `#![forbid(unsafe_code)]` test crate runs a full camera->display pipeline.
- M633: FreeRTOS executor proof, completing the bare/Embassy/FreeRTOS executor matrix; `libg2g_noalloc.a` linked into a plain C FreeRTOS app.
- M632: Embassy executor proof: the proof pipeline exposed as a plain future, awaited from a real Embassy task on QEMU.
- M631: audio render seam `PcmWriter`/`PcmSink` (`g2g-mcu`), the I2S/SAI-shaped write contract for interleaved S16LE samples.
- M630: camera capture seam `FrameGrabber`/`GrabberSrc` (`g2g-mcu`), the DCMI/CSI-shaped capture contract over a `StaticLendRing`.
- M629: first MCU peripheral element, `SpiDisplaySink` (ST7789/ILI9341), in a new heap-free `g2g-mcu` crate.
- M628: emulated Cortex-M execution proof: the heap-free pipeline boots and checksum-verifies on QEMU's MPS2-AN386 Cortex-M4.
- M627: build-time worst-case RAM/stack/ROM footprint report tool (`tools/footprint-report.sh`), enforced as CI budgets.
- M626: link-time panic-free proof: the heap-free pipeline archive contains zero `core::panicking` symbols.
- M625: link-time no-heap proof: a standalone `no_std` staticlib pipeline builds with no `#[global_allocator]` and no allocator symbols.
- M624: static (heap-free) element model: `StaticSource`/`StaticTransform`/`StaticSink` traits, const-arity runners, and a `Chain` combinator.
- M623: made `alloc` optional in `g2g-core`; the dynamic/build-time layer moves behind a new `alloc` feature, leaving a no-alloc data-plane default.
- M622: extended time newtypes to the PTP servo's reference-vs-master seam; a new `RefNs` types the monotonic reference distinct from absolute `TaiNs`.
- M621: grew the conformance matrix to a real interop+hardware matrix (ffmpeg-peer Oracle evidence, GPU-tagged Hardware evidence) and added the CI job publishing it.
- M620: extended the no-steady-state-allocation proof from a single stage to a multi-stage source->transform->sink pipeline.
- M619: added a second conformance oracle, the native MPEG-TS muxer (`TsMux`) validated via ffprobe.
- M618: boundary-scoped time newtypes (`TaiNs`, `RtpTs`) at the clock/PTP/RTP seam, so the compiler rejects mixing them.
- M617: made zero-copy an enforced graph-level contract via `run_graph_with_copy_policy`, checked against the M613 copy plan before frames flow.
- M616: proved the no-steady-state-allocation embedded contract on the capture path with a counting `#[global_allocator]`.
- M615: conformance matrix gains persisted interop/hardware evidence via an append-only log, so resource-owning tests can lift derived maturity.
- M614: conformance harness + derived maturity metadata: `MaturityLevel` is derived from an evidence bag, never hand-set.
- M613: copy/allocation plan (`g2g-core/src/copyplan.rs`): static analysis of a graph's per-edge memory-domain transfers, enforceable via `CopyPolicy`.
- M612: full per-format ST 2110-21 VRX validator (`VrxValidator`), a leaky-bucket receiver-buffer model replacing the burst-tolerance approximation.
- M611: reused the ST 2110-21 `Pacer` in the -22 JPEG XS sink via a shared `pace_send` helper; adds a `pacing` property.
- M610: wired ST 2110-7 seamless protection into the -20 video source via `RedundantRtpReceiver` and new `redundant*` properties.

- M609: ST 2110-21 sender pacing: `st2110pacing::Pacer` spreads a frame's RTP packets across the period (linear or gapped); `St2110VideoSink` gains a `pacing` property.
- M608: ST 2110-7 seamless protection: `st2110dup::SeamlessDedup` merges duplicate RTP streams from two paths, forwarding the first copy of each sequence. SDP gains `a=group:DUP` grouping.
- M607: `AudioFormat::PcmS24Le` (24-bit signed PCM, 3 bytes LE), the integer sibling of `PcmF32Le` for the ST 2110-30/AES67 L24 wire.
- M606: multi-section ST 2110 session SDP (`St2110Session`) describes a whole program (video+audio+ancillary) in one document instead of one SDP per stream.
- M605: JPEG XS encode/decode via Intel SVT-JPEG-XS (`SvtJpegXsEnc`/`SvtJpegXsDec`, `g2g-plugins`, `jpegxs` feature, Linux), the actual -22 codec.
- M604: adds ST 2110-22 JPEG XS over RTP (RFC 9134): sans-IO packetizer/depacketizer, st2110jxsrtp UDP sink/src, SDP jxsv support.
- M603: adds SDP auto-config for ST 2110 audio and ancillary sources via apply_sdp()/sdp(), completing self-describing streams for all three essences.
- M602: adds L24/F32 audio to ST 2110-30 (St2110AudioSink/Src), with pcm_to_samples/samples_to_pcm/wire_depth helpers for format<->wire conversion.
- M601: adds ST 2110 SDP (RFC 4566 + ST 2110-10/-20/-30/-40) generator/parser, wiring -20 video sink/src to sdp()/apply_sdp() auto-config.
- M600: adds ST 2110-20 10-bit 4:2:2 (RFC 4175) via a per-pgroup Layout seam; St2110VideoSink/Src support i422_10le.
- M599: adds ST 2110-20 uncompressed video over RTP (RFC 4175): sans-IO packetizer/depacketizer for RGBA and YCbCr-4:2:2 8-bit, plus UDP sink/src.
- M598: adds ST 2110-40 caption network elements (St2110AncSink/Src) bridging CEA-608/708 captions over RTP via CDP; shares decode core with CcExtract.
- M597: adds ST 2110-30 audio network elements (St2110AudioSink/Src) over UDP, PTS via the elected clock's media-clock timestamp.
- M596: adds ST 2110-40 ancillary data over RTP (RFC 8331): sans-IO packetizer/depacketizer for SMPTE ST 291 ANC packets (captions, timecode, AFD).
- M595: adds MediaClock (ST 2110-10) mapping RTP to PTP, and st2110audio, a sans-IO ST 2110-30 PCM packetizer/depacketizer (RFC 3190 L16/L24).
- M594: adds an in-process software PTP client (SLAVE mode): wire parser, PtpSlave state machine, and a UDP PtpClient transport.
- M593 phase C: adds PtpSystemClock, an OS-delegate PTP backend reading CLOCK_TAI/CLOCK_MONOTONIC pairs for linuxptp hosts.
- M593 phase B: adds the PtpClock type and a PtpGrandmaster election tier above LiveSource, offered only once the servo locks.
- M593 phase A: adds PtpServo, a PTP (IEEE 1588/ST 2059-2) clock servo computing offset/mean-path-delay and disciplining a DriftClock, with lock/holdover states.
- M592: adds A/V lip-sync validation: a two-arm graph test confirms video slaves to the elected audio clock, not wall time.
- M591: adds AlsaSink DAC-disciplined playout clock (AudioProvider priority) feeding a DriftClock; bounds the ALSA ring buffer; adds MonotonicClock.
- M590: adds DriftClock, a disciplined pipeline clock that fits master playout time to a reference clock by least squares for A/V sync.
- M589: adds a Linux audio-sink host smoke test driving AlsaSink/PulseSink/PipeWireSink against a real device across format/channel combos.
- M588: VulkanVideoPlayer scrubs H.265 and seeks to the nearest IRAP (IDR/CRA/BLA) random-access point, not just IDR.
- M587: H265DpbDecoder discards RASL leading pictures on mid-stream random-access tune-in at a CRA, avoiding decode against a flushed DPB.
- M586: VulkanVideoDec reorders B-frames to display order on the streaming (per-AU) NV12 path via a ReorderBuffer keyed by (sequence, POC).
- M585: adds a searchable web element reference (docs/elements.html) via new Registry::describe/describe_all introspection and a g2g-docgen binary.
- M584: scriptrouter route() may return multiple output ports to fan a buffer out; adds Frame::share() as the shared fan-out primitive.
- M583: adds scriptrouter, a Rhai-scripted 1-to-N routing demux that sends each frame to an output port chosen by script.
- M582: adds native bulk frame ops (invert/fill/apply_lut) to scriptelement for speed; fixes an EOS-forwarding bug that could deadlock a run.
- M581: scriptelement edits frame bytes in place via a zero-copy FrameBuf handle instead of a copied-in/out blob.
- M580: adds scriptelement (script-rhai feature), a raw-video transform whose per-frame process(frame) is a Rhai function.
- M579: adds Rhai graph-building scripts via a builder API (add/caps/set/link/link_leaky) sharing the M578 GraphSpec.
- M578: adds a declarative JSON/YAML graph format (nodes+edges) that builds a runnable Graph through the same registry/role/caps machinery as parse_launch.
- M577: fixes H.265 open-GOP (CRA/RASL) decode: H265DpbDecoder now flushes the DPB only when NoRaslOutputFlag is 1, not on every IRAP.
- M576: VulkanHdrSink present now uses GPU semaphores (image_available/render_finished/in_flight) instead of CPU fence waits between acquire/blit/present.
- M575: adds HDR swapchain present (hdr-present feature): VulkanHdrSink owns a raw VK_KHR_swapchain and presents via vkCmdBlitImage with HDR10/scRGB colour-space negotiation.
- M574: adds PQ/HLG HDR->SDR tone-mapping to the GPU-texture converter (BT.2390 EETF, transfer stage selected by a push constant).
- M573: adds a 10-bit YCbCr->RGBA GPU-texture path: 10-bit frames sample through a 10-bit VkSamplerYcbcrConversion into an Rgba16Float texture.
- M572: adds 10-bit AV1 hardware decode (Main profile TYPE_10, G10X6 output), reusing the M571 bit-depth machinery.
- M571: adds 10-bit HEVC (Main 10) hardware decode: session derives bit depth from the SPS and selects the Main 10 profile + two-plane 10-bit output format.
- M570: adds colour-space-aware YUV->RGB conversion driven by the stream's VUI/color_config (BT.601/709/2020, studio/full range) instead of a fixed BT.601 matrix.
- M569: H.264/H.265 decode reorders B-frames to display order via reorder_to_display_order, keyed by (sequence, POC); streaming path stays coding-order.
- M568: AV1 film grain now applies on the GPU-texture decode path via a per-slot readback + CPU grain synth + re-upload.
- M567: fixes Vulkan Video AV1 loop-restoration decode: LoopRestorationSize was mapped to the unit pixel size instead of 1+lr_unit_shift.
- M566: adds Vulkan Video AV1 film grain synthesis on the decoded NV12 (parse_av1_film_grain + apply_film_grain_nv12), ported from re_rav1d.
- M565: adds Vulkan Video AV1 alt-ref + show_existing_frame decode via a reorder-aware decode path and two new DpbCore slot-emit primitives.
- M564: adds Vulkan Video AV1 multi-tile decode: av1_tile_layout parses tile_group_obu headers into the driver's tile offset/size arrays.
- M563: adds MPEG-4 Part 2 (Visual) software decode via libavcodec, with MP4/TS demux support for mp4v/esds and elementary-stream framing.

## 0.2.0 - 2026-07-06

First tagged release. Closes the 1.0 extensibility gate (`#[non_exhaustive]` vocabulary,
M561) and publishes the stability policy (`STABILITY.md`). Contains all milestone work to
date; `g2g-core`'s execution surface is de-facto frozen and treated as Tier 1.

- M562: adds zero-stall external-semaphore sync for the GPU dma-buf egress path via an exportable VK_KHR_external_semaphore_fd timeline semaphore.
- M561: marks growth-prone public vocabulary enums #[non_exhaustive] and adds wildcard match arms across ~100 call sites, the 1.0 extensibility gate.
- M560: adds NV12/planar export to WgpuToDmaBuf, a full cross-process GPU-egress example, and validates external-semaphore sync as a primitive.
- M559: adds WgpuToDmaBuf, a GPU->DMABUF export element pairing with DmaBufToWgpu for zero-copy GPU frame handoff across processes.
- M558: adds reconnection to the distributed-graph TCP/WebSocket transports: deferred connect with backoff, mid-stream reconnect, and keep-listening sources.
- M557: adds local-dmabuf, a vendor-neutral DMABUF transport (DmaBufSink/DmaBufSrc) passing dma-buf fds via SCM_RIGHTS over a Unix socket.
- M556: adds local zero-copy IPC over CUDA IPC handles (localipc, LocalCudaSink/LocalCudaSrc), including a zero_copy() mode with no receive-side copy.
- M555: adds RemoteWsTransform/WsWireTransform, a generic remote-transform primitive offloading a single graph stage over WebSocket; retires the WebRemoteDetect shim.
- M554: adds the distributed-graph primitive over WebSocket (RemoteWsSink/RemoteWsSrc, browser WsWireSink), the WebSocket sibling of the TCP transport.
- M552/M553: adds GPU-texture and pipelined-streaming test coverage for H.265/AV1 in the re_video Vulkan Video adapter, closing codec test-parity gaps.
- M551: adds a distributed-graph primitive: a no_std wire codec for PipelinePacket plus RemoteSink/RemoteSrc TCP elements to run a subgraph in another process.
- M550: adds a portability showcase proving one graph (detection + overlay) runs unchanged across MCU/RTOS/CPU/GPU/WASM targets.
- M549: adds WebRemoteDetect, a thin browser client that ships frames to a native detection server over WebSocket and reattaches returned boxes as AnalyticsMeta.
- M548: adds WebOrtDetect, real in-browser YOLOv8 detection via ONNX Runtime Web, decoded through the shared DetectionPostprocess; fixes a WebCodecsDecode dimension bug.
- M547: adds WebDetect, an in-browser detection-chain stage attaching AnalyticsMeta from a synthetic tensor decoded via DetectionPostprocess.
- M546: adds a real 2-layer CNN head (conv3x3->ReLU->conv3x3->ReLU) to the browser WebGPU inference path, zero-copy from the decoded external texture.
- M545: adds browser GPU inference: WebGpuCanvasSink runs a per-pixel nearest-centroid classifier directly over the decoded frame in a WGSL fragment shader.
- M544: adds WebCameraSrc, real webcam capture via getUserMedia/MediaStreamTrackProcessor feeding the browser egress pipeline.
- M543: fixes four browser pipeline startup bugs (caps negotiation, CapsChanged handling, canvas dimension mismatch, socket reopen) so it runs end to end.
- M542: adds browser egress: WebCodecsEncode, WebSocketSink, and PatternSrc let a wasm pipeline capture, encode, and send H.264 over WebSocket.
- M541: adds WebGpuCanvasSink, zero-copy WebGPU presentation of WebCodecsDecode's GPU-resident output via GPUExternalTexture, no CPU readback.
- M540: adds g2g-web, a deployable browser (WebAssembly) pipeline cdylib with WebSocket/WebRTC entry points, plus a runnable demo harness.
- M539: adds pipelined streaming decode to the Vulkan Video system path (decode_push/decode_flush) so the element keeps the decode queue saturated across AUs.
- M538: chains Vulkan Video texture decode to its ycbcr conversion with a GPU semaphore, removing the mid-picture CPU fence wait (~1.9x throughput).
- M537: persists the Vulkan Video NV12->RGBA compute converter (YcbcrConverter) instead of rebuilding the pipeline every picture (~369fps at 640x480).
- M536: pipelines the Vulkan Video system-memory decode path through an in-flight ring, retiring slots only when the ring wraps (~16% higher throughput).
- M535: validates the zero-copy GPU-texture decode wedge (VulkanVideoDec->WgpuSink) for all three codecs (H.264/H.265/AV1) on the RTX 3060.
- M534: adds macOS VideoToolbox HEVC (H.265) encode + decode alongside the existing H.264 path, sharing the session and NV12 packing.
- M533: adds RTMP window-acknowledgement back-pressure: RtmpSession emits Acknowledgement per window, RtmpSink blocks while throttled.
- M532: RTSP TCP-interleaved ingest via SETUP `interleaved=N-M`; `RtspServerSrc` demuxes `$`-framed RTP off the control connection and depayloads it.
- M531: adds `WgpuInference::add` (residual op) and `StackLayer::SaveSkip`/`AddSkip` for `y = f(x) + x` blocks; safetensors loader now dequantizes F16/BF16 to f32.
- M530: adds `MultiDuplexSession::reverse_channel(input)`; `WebRtcDuplexSession` routes a remote per-mid PLI/BWE event back to the send source feeding that track.
- M529: fixes `accept_and_handshake` dropping the RECORD control `TcpStream`, which closed the RTSP control connection and broke ffmpeg ingest interop; `run` now holds it alive across the receive loop.
- M528: validates `UdpSrc`'s RFC 6184 depayloader and jitter buffer against ffmpeg's RTP H.264 stream, including FU-A fragmentation of a large IDR.
- M527: validates `RtmpSrc`'s handshake, chunk reassembly and AMF0 publish flow against ffmpeg's RTMP client. Also makes `RtmpSrc::from_listener` mark the element configured.
- M526: adds AES-128/256 reverse-direction SRT interop tests against an ffmpeg listener, completing the full 6-case (direction x encryption) matrix.
- M525: fixes `SrtSink` addressing its conclusion handshake to the wrong socket id, which libsrt silently dropped; it now sends to dst 0 and learns the real id from the conclusion response. Also bounds the whole handshake with an 8s deadline.
- M524: adds `WgpuInference::batch_norm`, a general `linear_shaped`, and `stack_from_safetensors`/`StackLayer` to run a full CNN (conv-BN-ReLU-pool x2 -> global-avg-pool -> linear) GPU-resident from one weight file.
- M523: adds `g2g-core` `ReverseChannel` and `MultiInputElement::reverse_channel(input)`; `WebRtcSessionSink` routes a remote PLI/BWE event by `mid` to the source feeding that track.
- M522: fixes `SrtSrc`'s listener echoing `KMREQ` instead of `KMRSP` in its conclusion response, which libsrt rejected; `Handshake` gains an `is_response` flag to pick the right TLV.
- M521: adds `rtmphandshake` (HMAC-SHA256 genuine FMS/FP complex handshake); `RtmpPublisher`/`RtmpSession` negotiate it by default, falling back to the simple echo against a non-genuine peer.
- M520: gives `RtspServerSrc` a receive-side jitter buffer by extracting `UdpSrc`'s jitter/RTCP/depayload loop into a shared `rtprecv::receive_rtp_h264` helper; adds `with_jitter`/`with_rtcp` builders.
- M519: fixes `VulkanVideoDec::ensure_decoder` ignoring a later keyframe's changed geometry; it now rebuilds the session and `DpbCore` for the new resolution and emits a fresh `CapsChanged`.
- M517: factors `H264DpbDecoder`/`H265DpbDecoder`/`Av1DpbDecoder` onto a shared `DpbCore` (device/session/DPB pool/command recording); `VulkanVideoDec` generalizes over a codec enum so one element decodes H.264/H.265/AV1.
- M516: adds a `gpu_texture: Option<G2gGpuFrame>` field to the `re_video` fork's `DecodedFrameContent`, so GPU mode decodes an mp4 straight into a GPU-resident RGBA texture with no CPU readback in the decode path.
- M515: adds a `pub use wgpu` re-export and public `gpu::read_rgba_texture(ctx, texture)` helper, so a downstream consumer (the `re_video` fork) can name g2g's exact wgpu version and verify a shared texture.

- M514: the `re_video` fork's g2g backend pulls `avcC`/`hvcC`/`av1C` from the mp4 `stsd` box via `extract_config` and builds via `VulkanStreamDecoder::from_config`, falling back to re_video's software path when no Vulkan Video device opens. No g2g crate changes (fork + docs only).

- M513: adds `VulkanStreamDecoder::from_config` building from an out-of-band `avcC`/`hvcC`/`av1C` record and reframing length-prefixed AVCC samples to Annex-B. Fixes a bug where a 4-byte NAL length in 256..=511 sniffed as an Annex-B start code and decoded to garbage.

- M512: adds `VulkanStreamDecoder::new_gpu`/`submit_chunk_texture` returning each decoded frame as a GPU-resident RGBA `wgpu::Texture` (via the M494 ycbcr compute pass), no CPU readback in the decode path. Adds `gpu_context()` and a `WrongOutputMode` error for the mutually-exclusive CPU/GPU modes.
- M511: adds cross-thread decode-determinism tests proving H.264/H.265 Vulkan decode is bit-identical on a spawned worker vs the main thread; AV1's equivalent is `#[ignore]`d for a driver-side residual off the main thread, not a g2g bug.
- M510: `open_decode_device` now enumerates Vulkan adapters and picks a discrete GPU passing the codec's decode probe, instead of trusting `request_adapter(HighPerformance)`. Also proves the M509 PoC double-free was re_video's detached thread racing process exit, not a g2g bug.
- M509: forks `re_video` (`g2g-vulkan` feature) with `impl AsyncDecoder for G2gVulkanDecoder` delegating to `VulkanStreamDecoder`; decodes AV1 bit-exact through the real crate API. Found a teardown double-free in re_video's pooled-thread `SyncDecoderWrapper`, not present on the inline main-thread path.
- M508: adds `g2g_plugins::revideo::VulkanStreamDecoder`, wrapping the H.264/H.265/AV1 DPB decoders in re_video's chunk-at-a-time `AsyncDecoder` contract (`new`/`submit_chunk`/`reset`), returning packed I420 frames with no re_video dependency. Adds `Av1DpbDecoder::reset`/`H265DpbDecoder::reset`.
- M506c: fixes `AV1_DEFAULT_LF_REF_DELTAS` for ALTREF/ALTREF2 (was `0`, spec requires `-1`), which mis-configured in-loop deblocking for compound blocks; all AV1 frames now bit-exact vs ffmpeg.
- M506b: adds `Av1DpbDecoder` mapping parsed headers onto `StdVideoDecodeAV1PictureInfo` and managing AV1's reference model directly. Intra and first single-reference inter frame are bit-exact vs ffmpeg; compound/temporal-MV inter frames carry a small residual (fixed in M506c).
- M506a: adds `parse_av1_frame_header`, reading the full AV1 uncompressed header (tile/quant/segmentation/loop-filter/CDEF/LR/global-motion) since Vulkan Video AV1 decode needs it pre-parsed. Validated field-by-field against `ffmpeg trace_headers`.
- M507: adds a byte-budgeted eviction bound (`cache_byte_budget`, default 512 MiB) to `VulkanVideoPlayer`'s frame cache alongside the frame-count LRU, and an opt-in `set_cache_traversed` mode caching every run-up picture for cache-hit backward scrubs.
- M505: adds `open_av1_decode_device` and `VulkanVideoDevice::create_av1_session`, carrying the Std AV1 sequence header via `VkVideoDecodeAV1SessionParametersCreateInfoKHR`; the driver validates the M504 `Std*` mapping on session creation.
- M504: adds `av1_obus` (LEB128 OBU walk), `parse_av1_sequence_header`, and `to_std_av1_seq_header` mapping onto `StdVideoAV1SequenceHeader`; `av1_frame_infos` classifies each coded frame. Verified against ffmpeg `trace_headers` on a libaom fixture.
- M503: adds `H265DpbDecoder`, parsing each slice header, computing POC, and applying the reference-picture set to the DPB; decodes bit-exact vs ffmpeg. Fixes an NVIDIA driver quirk needing a 3-byte start code (not 4) for non-IDR slices.
- M502: adds `open_h265_decode_device` and `VulkanVideoDevice::create_h265_session`, carrying the Std VPS/SPS/PPS from `StdH265Params`; the driver validates the M501 mapping on session creation. Generalizes `decode_format` for both codecs.
- M501: adds `parse_h265_vps`/`sps`/`pps` and `to_std_h265_params` mapping onto `Std*` layout, including short-term RPS explicit-form conversion. Main profile 4:2:0/4:2:2/4:4:4 8-bit; unsupported features rejected at parse.
- M500: proves a foreign wgpu pipeline built on the shared decode device (`VulkanVideoPlayer::gpu_context`) can bind and sample the decoder's imported texture zero-copy in its own render pass, no decoder change needed. Adds `examples/vulkan_video_engine_embed` for a visual check.
- M499: adds a forward-continue fast path (linear playback decodes each picture once, not O(n^2)), an LRU cache bound (`cache_capacity`, default 64), and fixes multi-GOP display order (sorted by (GOP, POC), not POC globally).
- M498: adds `VulkanVideoPlayer`, a pull-based scrubber indexing keyframes/POC via `H264DpbDecoder::index_pictures` and serving `frame_at_index`/`frame_at(pts)` as a cached GPU-resident texture, decoding from the enclosing keyframe on a cache miss. Adds an interactive `examples/vulkan_video_scrubber`.
- M497: adds `examples/vulkan_video_on_screen`, presenting decoded H.264 via `WgpuSink` in a real window with zero-copy present when the decode GPU drives the display, and a cross-GPU readback fallback on hybrid/PRIME hosts. Validated live on Wayland and X11.
- M496: registers `vulkanvideodec` in `register_autoplug_candidates` tagged `produces(WgpuTexture)` + `hardware()`, so a WgpuTexture-preferring `decodebin`/`playbin` search picks it for H.264; it remains a valid NV12 fallback for a plain System search.
- M494/M495: adds `H264DpbDecoder::decode_all_to_textures`, converting each decoded DPB slot in place via the ycbcr compute pass into an RGBA texture; `VulkanVideoDec` becomes a multi-domain producer negotiating `WgpuTexture` vs system NV12 with the downstream sink.
- M493: adds `VulkanVideoDec`, the `AsyncElement` wrapper around `H264DpbDecoder`: `Caps::CompressedVideo{H264}` in, `Caps::RawVideo{Nv12}` out, DPB state carried across `process` calls. Registered as `vulkanvideodec`. Adds `examples/vulkan_video_smoke` dumping PPM frames.
- M492: adds `H264DpbDecoder` with slice-header parse, POC computation and sliding-window reference marking, decoding P frames bit-exact vs ffmpeg. Fixes a `parse_h264_pps` bug (missing `more_rbsp_data()` check) that had corrupted every prior decode to flat mid-grey.
- M491: adds `decode_idr_to_rgba_texture_gpu`, converting NV12 to RGBA via a `VkSamplerYcbcrConversion` compute pass and importing the result straight into wgpu, with no CPU round-trip. Reuses the Android `mediacodec-wgpu` ycbcr shader.
- M490: adds `VulkanVideoDevice::decode_idr_to_rgba_texture`, decoding the IDR, converting NV12 to RGBA on the CPU (BT.601 limited range), and uploading to a wgpu texture on the decode device's own queue. Adds `read_rgba_texture` for validation.
- M489: adds `VulkanVideoDevice::decode_idr_luma`, the first end-to-end Vulkan Video decode: builds the DPB/output image and bitstream buffer, and runs `vkCmdDecodeVideoKHR` for the first IDR (constants only, no slice-header parse yet).
- M488: adds `open_h264_decode_device` (a wgpu device with a `VK_QUEUE_VIDEO_DECODE_BIT_KHR` queue) and `VulkanVideoDevice::create_h264_session`, carrying the Std SPS/PPS from M487; the driver validates the mapping on session creation.
- M487: adds `parse_h264_sps`/`pps` and `to_std_sps`/`to_std_pps` mapping onto the `Std*` layout since Vulkan Video does not parse the bitstream itself. Baseline/main/high 4:2:0 8-bit; unsupported features rejected at parse.
- M486: adds the `vulkan-video` feature and `probe_decode_caps(codec)`, finding a `VK_QUEUE_VIDEO_DECODE_BIT_KHR` queue family and querying decode limits (coded-extent range, DPB slot budget, alignments) for H.264/H.265/AV1.
- M485: `parse_caps_set` now accepts GStreamer range/list caps syntax (`[1,1920]`, `{I420,NV12}`); `queue max-size-buffers=N` sets a per-edge capacity override via new `Edge.capacity`/`Graph::link_full`.
- M484: `parse_launch` now handles `#` comments and single-quoted values, gives a clearer caps-filter error, and fixes stale `gst_compat` NVENC/NVDEC mappings (`nvh264enc`/`nvh265enc` -> `nvenc`, `nvh264dec`/`nvh265dec` -> `nvdec`).
- M483: adds `run_graph_threaded`, running each arm on its own OS thread via a `GraphSpawner` (`ThreadSpawner` or `TokioThreadSpawner`) instead of cooperative multiplexing; selected via `g2g-launch --threads`, cooperative stays the default.
- M482: adds `DecodebinSelectHook` so `decodebin name=d` fan-out probes the container and auto-plugs a decoder onto each requested `video_0`/`audio_0`/`text_0` port, decoding every branch to raw frames.
- M481: adds `MultiInputElement::input_pad_index(req, ordinal)` so a gst-launch line can route to a muxer's inputs by name (`o.video`, `m.audio_0`) instead of branch order; bare `m.` refs stay positional.
- M480: adds `SourceLoop::probe_output_caps`; `FileSrc` runs the `auto` header sniff at parse time so `decodebin` picks the demuxer from real file content even when the extension lies.
- M479: adds `ByteStreamEncoding::Mp4` (whole-file, distinct from streaming `IsoBmff`) and a single-output `Mp4Demux` element registered as `qtdemux`, so a plain (non-fragmented) MP4 runs through a linear `filesrc ! decodebin`/`qtdemux` chain.
- M478: adds MP4 detection to `typefind::sniff`, a new `typefind::sniff_caps` for subtitle documents, and extension-based typing (`FileSrc::untyped`/`caps_from_extension`) so a bare `filesrc` types itself with no `bytestream-format` hint.
- M477: adds `subparse` as a launch element and `TextOverlayN` fan-in muxer for subtitle-overlay lines; the M476 demux fan-out also selects embedded subtitle tracks (`d.text_0`) for Matroska and MP4.
- M476: adds a `DemuxSelectHook` registry slot so a named demuxer fed by a file source (`matroskademux name=d  d.video_0 ! ...`) splits into elementary streams by `video_N`/`audio_N` pad name instead of erroring on fan-out.
- M475: adds `with_stream_select(StreamSelectController)` to `TsDemuxN` and `Mp4DemuxN`, matching `MkvDemuxN`, so the app can re-map demuxer ports to named stream ids mid-stream.
- M474: adds `examples/gst_equivalents.rs`, running each PORTING.md recipe both via `parse_launch` and as a hand-built typed `Graph`, asserting matching frame counts.
- M473: `parse_launch` now splices a broadcast `tee` onto a non-tee node's output when it feeds multiple branches, instead of erroring `FanOutWithoutTee` (removed). Per-branch `queue` policies are preserved.
- M472: adds `MultiInputElement::metadata()` so `g2g-inspect` can construct a fan-in muxer instance and print its properties/caps, not just the name and role.
- M471: gives fan-in muxer variants the same runtime properties as their single-input siblings: `tsmuxn` gains `pat-interval`/`pmt-interval`, `mkvmuxn` gains `streamable`, `mp4muxn` gains `fragment-duration` (needing real multi-sample fragmentation).
- M470: adds a README "Coming from GStreamer?" on-ramp and a PORTING.md equivalence cookbook backed by the M469 corpus; fixes the stale "spaces in property values fail" note.
- M469: adds `tests/gst_launch_corpus.rs`, checking a portable-lines set parses/runs/lints clean and a needs-porting set (`theoraenc`, `x265enc`) is flagged by `lint_launch`.
- M468: `g2g-launch -v` now prints each link's negotiated caps, memory domain and `LinkPolicy` (via a throwaway `negotiate_graph` copy), instead of just the topology.
- M467: `runtime::parse_launch` now tokenizes double-quoted values as one token, so whitespace and `!` inside a value are literal (`gstwrap element="x264enc bitrate=4000"`), replacing the previous whitespace-split shortcut.
- M466: adds `gstwrap`, an `AsyncElement` that hosts a real un-ported GStreamer element inside a g2g graph via `appsrc ! <element> ! appsink` (a `gstwrap_host.c` C ABI helper), the mirror of `g2g-bridge`. System-memory copy each way; gated behind a new `gstreamer` feature.
- M465: wires the GStreamer bridge shell's `generate_output` override to import a dma-buf `GstBuffer` on input (`g2g_bridge_push_dmabuf`) and wrap a dma-buf output frame back via `gst_dmabuf_allocator_alloc`, a full zero-copy round-trip.

- M464: adds `dmabuftowgpu`, importing a `DmaBuf` frame into a GPU-resident `wgpu::Buffer` via `VK_EXT_external_memory_dma_buf`, no CPU copy. Validated on an RTX 3060 by exporting and re-importing device-local GPU memory as a dma-buf fd.

- M463: adds `AppSrcFeed::push_dmabuf` and C-ABI `g2g_bridge_push_dmabuf(bridge, fd, stride, offset, pts)`, importing a dma-buf fd into the bridge with no pixel copy. Not yet auto-routed from the shell's default transform.

- M462: adds `gst_compat::scan_source` behind `g2g-inspect --gst-scan <file>`, statically scanning a GStreamer C/Python app for unportable element factories and dynamic-pipeline idioms (`pad-added`, appsrc/appsink).

- M461: `gst_compat::lint_launch` now scans every `!`-separated segment and reports all unportable elements in a line at once, instead of stopping at the first the parser trips on.
- M460: gst_compat lint points x264enc/x265enc hints at ffmpeg/nvenc/mfencode; PORTING.md refreshed and gains §5.1 mapping dynamic-pipeline idioms to g2g primitives.

- M459: glass2glass gst element gains an output-caps property, non-in-place transform, and BridgeGraph::with_output_caps, so a fragment can rescale or reformat.

- M458: adds libgstglass2glass.so, a GstBaseTransform shell wrapping BridgeGraph as the glass2glass element; fixes an appsink bug dropping frames after a caps-changing upstream transform.

- M457: adds g2g-bridge's BridgeGraph, embedding a g2g sub-graph on its own thread behind a synchronous push/pull API for a GStreamer app's chain function.

- M456: mp4mux gains fragment-duration (batches AUs into keyframe-aligned moof+mdat fragments); matroskamux gains streamable (omits Cues in live mode). flvmux streamable skipped.

- M455: h264parse/h265parse gain config-interval (re-inserts cached SPS/PPS/VPS before a keyframe); mpegtsmux gains pat-interval/pmt-interval. mp4mux faststart skipped.

- M454: exposes missing properties across many encoders, sources, sinks, and overlays so parse_launch can configure them.

- M453: new detect_overlay example draws OrtInference+DetectionPostprocess boxes back onto the frame via AnalyticsOverlay, with a still-PPM path and a live Wayland path.

- M452: a full YOLO detector runs as one negotiated graph, TensorSource->OrtInference->DetectionPostprocess->DetectSink, via run_linear_chain, producing structured AnalyticsMeta detections.
- M451: fixes OrtInference/TensorConvert/TensorPostprocess to forward CapsChanged as output, not reinterpret as input, so TensorConvert works as an interior runner node.
- M450: adds a runner-driven quantized-inference graph via run_linear_chain; folds quantize into the source as a workaround M451 later fixes.
- M449: adds from_memory_with_qnn and from_memory_with_coreml execution-provider constructors for OrtInference; target-only features, compile-checked, runtime validation pending hardware.
- M448: a real YOLO runs through OrtInference->DetectionPostprocess and detects the dog in the sample photo; model fetched on demand via gen.py.
- M447: gen_u8in.py re-quantizes MobileNetV2 to a QDQ uint8-input NNAPI form; on-device probe confirms the conv body places on the Edge TPU with correct top-1.
- M446: a real int8 MobileNetV2 classifies an image through OrtInference->TensorPostprocess::argmax, top-1 matching the CPU reference exactly; model fetched via fetch.py.
- M445: TensorConvert gains NCHW<->NHWC transpose and f32<->F16 narrowing/widening, composable with a dtype conversion in one pass; layout property added.
- M444: new on-device probe wires Camera2Src->quantize->OrtInference through the Edge TPU end to end, validated live on a Pixel 10a with a synthetic-frame fallback.
- M443: OrtInference accepts uint8/int8 tensor input directly (via with_tensor_input/infer_tensor_int), so a quantized model runs without an f32 boundary conversion.
- M442: gen_qconv.py retypes the model input to uint8 and drops the boundary QuantizeLinear, so the whole conv graph places on the Edge TPU with zero CPU nodes.
- M441: new TensorConvert element (g2g-plugins) quantizes/dequantizes an f32 tensor to int8/uint8 and back (scale + zero-point affine), no_std-safe, registered as tensorconvert.
- M440: a committed int8 QDQ Conv->ReLU fixture proves the Edge TPU actually executes a quantized conv, confirmed on a Pixel via ORT profiling and DarwiNN logcat.
- M439: adds NNAPI and XNNPACK execution-provider constructors for OrtInference, Android-target only, validated linking and running byte-exact on a Pixel.
- M438: HlsSrc starts near the live edge (~3 target durations back) instead of the oldest segment; with_full_replay() opts back into full-DVR playback from the front.
- M437: Mp4DemuxN demuxes a fragmented file progressively, parsing and emitting each moof+mdat as it arrives instead of buffering to Eos; a non-fragmented file still parses whole at Eos.
- M436: hls_playbin reads #closed-captions=/#cc= and overlays in-SEI captions for muxed-TS, fMP4, and separate-audio HLS variants, winning over an auto-selected subtitle rendition.
- M435: new Cc708Enc builds DTVCC window commands (DefineWindow, SetPenLocation, DisplayWindows) into service blocks/packets; CcInsert::cea708(service) selects 708 encoding alongside the existing 608 path.
- M434: CcInsert warns once when cues never reach a video AU; new cc_author example authors a subtitle file into a captioned MPEG-TS.
- M433: new SubtitleSrc reads a .srt/.vtt/.ssa/.ttml file and emits it as a Caps::Text{format} stream, sniffing format from extension or a format= property; registered as subtitlesrc.
- M432: new CcInsert element writes a GA94 caption SEI (via cea::build_cc_sei) into each H.264/H.265 access unit from a timed Text{Utf8} cue stream, using an internal Cc608Enc for pop-on/erase sequencing.
- M431: new Cc608Enc builds the pop-on CEA-608 command sequence (RCL/PAC/EOC/EDM) from pushed cues and queues cc_data byte pairs with control doubling and odd parity, drained one pair per frame.
- M430: file-container playbin hooks overlay in-SEI captions via a #closed-captions= URI fragment, teeing video into a CcExtract branch; overrides an auto-selected subtitle track.
- M429: new CcExtract element decodes CEA-608/708 SEI captions from a compressed video stream into timed Caps::Text{Utf8} cues, run in parallel with the decoder via a tee.
- M428: new Cea708 decodes the windowed DTVCC caption state machine from cc_type 2/3 triples into cues, with service filtering and bounds-checked parsing.
- M427: rebuilds Cea608 around a 15x32 grid, adding channel selection (CC1-4), pop-on/roll-up/paint-on modes, mid-row styling, and extended character sets, with placement derived from the grid.
- M426: new cea module extracts SEI cc_data triples from H.264/H.265 (extract_cc_data), and Cea608 decodes the field-1 CC1 pop-on path into subparse::Cue with bounds-checked parsing.
- M425: H265Parse gains a reframing() mode splitting HEVC into access-unit-aligned buffers; playbin auto-plugs opusdec for muxed Opus audio.
- M424: plain playbin A/V fan-outs (MKV/TS/MP4/HLS) now route audio through audioconvert/audioresample via new build_av_fanout instead of feeding the sink raw stream format.
- M423: Caps::Audio.channels gains an ANY_CHANNELS wildcard (intersect/is_fixed/fixate handle it); AudioConvert handles general N->M round-robin downmix/upmix, not just mono/stereo.

- M422: new FfmpegAudioDec decodes AAC via libavcodec to interleaved PcmS16Le; playbin wires decode->audioconvert->audioresample; HLS keeps a muxed AAC default rendition.
- M421: H264Parse gains a reframing() mode splitting PES buffers into access units, auto-spliced before decoders; fixes corrupt MPEG-TS/HLS playback. g2g-launch's tokio runtime now enables IO.
- M420: new build_hls_separate_subtitle_overlay joins a video source, a separate audio rendition, and a SUBTITLES rendition into one graph.
- M419: HlsSrc gains a text mode (with_text); new build_hls_subtitle_overlay joins a muxed A/V source with a separate WebVTT rendition source into the overlay's text pad via SubParse.
- M418: variant_streams surfaces SUBTITLES-group renditions; new pick_rendition selects by language; hls_playbin reads #audio-lang=/#subtitle-lang= hints.
- M417: MkvCodec maps S_TEXT/ASS and S_TEXT/WEBVTT to Subtitle(Ssa)/Subtitle(WebVtt); the demuxer de-frames each block to plain UTF-8 cue text.
- M416: fmp4::parse_text_entry recognizes wvtt and stpp sample entries alongside tx3g, de-framing to Text{Utf8}/Text{Ttml} for the existing mp4_playbin overlay.
- M415: mkv_playbin routes an embedded subtitle track through TextOverlayN, the Matroska sibling of M412; the overlay-assembly subgraph is factored into shared wire_subtitle_overlay.
- M414: ffmpegdec/ffmpegvaapidec now build the decoder in the chosen output format (NV12 or I420) instead of always I420, fixing a CapsMismatch on a strict-NV12 sink.
- M413: MkvCodec gains Subtitle(TextFormat); S_TEXT/UTF8 tracks forward as Caps::Text{Utf8} with BlockDuration-derived timing; kept out of the A/V fan-out.
- M412: mp4_playbin routes video through TextOverlayN fed by a tx3g subtitle track when both are present; falls back to the plain M392 fan-out otherwise.
- M411: fmp4::parse_all_tracks reads a tx3g timed-text track into TrackKind::Text, de-framing to Caps::Text{Utf8} with per-cue PTS/duration; kept out of forwardable_streams.
- M410: parse_webvtt reads STYLE ::cue/::cue(#id) color/background-color rules and resolves them onto each cue's CueSettings; both bitmap and TrueType overlay paths use the new fields.
- M409: new truetype-overlay feature gives TextOverlay a fontdue backend with a fallback face chain, rendering CJK/accented text; fontdue only rasterizes glyf outlines, not CFF.
- M408: resolve_forward_output forwards an element's single producible output caps with no downstream snapshot, fixing a decode->overlay CapsMismatch; new subtitle_player example.
- M407: new WritingMode on CueSettings, parsed from vertical:rl/lr; plumbed through SubParse/TextOverlayN, bitmap overlay still renders it horizontally.
- M406: new TextCueMeta carries a cue's CueSettings behind the metadata feature; SubParse attaches it and TextOverlayN reads it, so WebVTT/SSA placement survives.
- M405: SRT/WebVTT/SSA parsing in SubParse emits each cue as its block terminates instead of buffering to Eos; handles chunk-split multi-byte chars. TTML stays batch.
- M404: new MultiInputElement::output_follows_input lets the CSP solver derive a muxer's output caps from one of its input pads; TextOverlayN::new() becomes argument-free.
- M403: new TextOverlayN overlays a streamed Text{Utf8} cue pad onto a video pad via MultiInputElement, PTS-ordered merge, pairing with SubParse.
- M402: new subparse::parse_ttml parses W3C Timed Text <p> paragraphs (clock/offset times, entity decode, markup strip); SubParse and TextOverlay::from_ttml gain Text{Ttml} support.
- M401: new subparse::parse_ssa reads [Events] Dialogue lines by Format column order into timed UTF-8 cues; SubParse gains Text{Ssa}, TextOverlay::from_ssa handles .ass/.ssa.
- M400: adds Caps::Text/TextFormat as a first-class caps kind; new SubParse converts a Text{Srt}/Text{WebVtt} stream to timed Text{Utf8} cues, batch-parsed at Eos.
- M399: new runtime::ElementProbe/ElementLatency times every DataFrame process() call and samples input-link fill; RunStats::report() prints a measured p50/p99 + fill table per element.
- M398: fmp4::parse_all_tracks reads encv/enca cbcs sample entries; Mp4DemuxN::with_cenc_key decrypts each traf's senc subsample map before de-framing, failing loud without a decryptor.
- M397: hls_playbin builds two independent HlsSrc->TsDemuxN->decode->sink chains, merged via Graph::merge, when a variant binds a separate AUDIO-group rendition.
- M396: hls_playbin picks Mp4DemuxN for an fMP4 (#EXT-X-MAP) variant via new build_hls_fmp4_fanout, otherwise stays on the muxed-TS path; fans out as VOD since Mp4DemuxN batches on EOS.
- M395: hls parser reads #EXT-X-MEDIA renditions and group bindings; new hls_playbin hook probes a hls:// master and fans out a muxed-TS variant via HlsSrc->TsDemuxN->{decode->sink}.
- M394: Mp4DemuxN re-frames demuxed AAC access units to ADTS from the track's esds AudioSpecificConfig, so a decoder can start without out-of-band config.
- M393: new fmp4::parse_progressive_multi demuxes a classic moov+mdat multi-track file by track_ID; Mp4DemuxN picks it when the file has no moof.
- M392: new mp4_playbin hook probes a fragmented MP4's moov and fans out FileSrc->Mp4DemuxN->{decode->sink}, declining a container whose moov is outside the probed prefix.
- M391: new multi-output Mp4DemuxN demuxes a fragmented MP4 into per-track elementary streams, de-framing video AVCC->Annex-B and passing audio through, with runtime CapsChanged refinement.
- M390: new fmp4::parse_all_tracks/parse_fragments_multi parse every moov/trak into TrackHeaders and route moof+mdat fragments by track_ID, the read-side groundwork for Mp4DemuxN.
- M389: `playbin uri=*.ts` auto-fans-out via new `ts_playbin` hook (MPEG-TS, alongside `mkv_playbin`); HLS reuses it via `TsDemuxN`.
- M388: adds multi-output MPEG-TS demuxer `TsDemuxN`, routing PES units to ports by codec and announcing a `StreamCollection`.
- M387: adds `gapless_playbin` builder: one call wires a playlist into `GaplessSrc` + auto-plugged decode to a sink.
- M386: `TsDemux`/`Mp4Src` now announce their streams via `BusMessage::StreamCollection`, extending M376 past Matroska.
- M385: adds `AvOffset` transform, shifting a stream's PTS/DTS by a signed offset (ns) to re-align A/V branches.
- M384: adds instant URI switch on `GaplessSrc` via `GaplessController::switch_now`, flushing and preempting the current item.
- M383: adds gapless playback: `GaplessController` + `GaplessSrc` concatenate a playlist with rebased PTS/DTS and no flush.
- M382: `playbin uri=X` in `parse_launch` auto-fans-out an MKV file via a new `Registry` `PlaybinHook` (`mkv_playbin`).
- M381: adds `MkvDemuxN::with_stream_select`, letting the app remap which stream each output port carries at runtime.
- M380: multi-output demux ports declare per-port caps (`port_output_caps`); the graph solver negotiates each branch's decoder.
- M379: adds `Registry::build_playbin_graph`, assembling `source -> demux -> {decode -> sink}` per selected stream.
- M378: adds multi-output Matroska demuxer `MkvDemuxN`, routing each track's access units to its own output port.
- M377: adds `StreamSelectController` + `MkvDemux::with_stream_select` for app-driven stream selection, confirmed via `StreamsSelected`.
- M376: adds stream discovery: `StreamCollection`/`Stream` types and `BusMessage::StreamCollection`; `MkvDemux` announces every track.
- M375: the Matroska muxer now writes a `Cues` index at EOS, closing the write side of the M373 seek round-trip.
- M374: `MkvDemux` seeks via a `SeekHead`-located `Cues` prefetch when the index itself isn't parsed yet.
- M373: `MkvDemux` seeks via a parsed `Cues` index, byte-seeking straight to the target Cluster instead of re-scanning.
- M372: adds throughput-driven ABR to `DashSrc` (`with_abr()`), reusing the shared `abr::BandwidthEstimator`.
- M371: adds throughput-driven ABR to `HlsSrc` (`with_abr()`) via a new EWMA `abr::BandwidthEstimator`.
- M370: adds DASH `SegmentBase` (sidx-indexed single-file CMAF) support, parsing `sidx` for byte-range subsegments.
- M369: adds DASH `SegmentList` byte-range addressing, generalizing `Representation` into a `SegmentSource` enum.
- M368: adds HLS `#EXT-X-BYTERANGE` support, fetching only the addressed sub-range via a new `fetch::get_range_bytes`.
- M367: `HlsSrc`/`DashSrc` gain `with_seek`, resolving a TIME seek to the containing media segment and re-emitting init.
- M362-M366: byte-stream demuxers (`Fmp4Demux`, `TsDemux`, `MkvDemux`, `FlvDemux`, `OggDemux`) gain seek via a shared `DemuxSeek` helper driving the upstream byte source.

- M361: `FileSrc` gains `with_seek`, a byte-offset seek that flushes and repositions the file read.

- M360: adds re-preroll after a flushing seek issued while paused, via `StateController::request_repreroll()`.

- M359: `Mp4Src` loops a clip gaplessly on a `SeekFlags::SEGMENT` seek; the idle park is now wakeful, not polling.

- M358: adds `SeekFlags::SEGMENT` playback/looping via a segment-done back-channel on `SeekController` plus `shutdown()`.

- M357: `g2g-pyapi` `Pipeline::wait` now surfaces the specific `G2gError` (or panic message) instead of one opaque failure.

- M356: `g2g.MetaSink` (embedded Python) becomes a plain `Sync` `pyclass` over a `Mutex`, safe to call from any thread.

- M355: caps the adaptive-streaming HTTP body read (`fetch::get_bytes`/`get_text`) to bound memory against a hostile server.

- M354: adds domain-converter auto-plug (`auto_plug_domain_converters`), splicing a converter onto an edge with no shared memory domain.

- M353: adds `CudaUpload` (System NV12 -> CUDA), the host-to-device converter feeding `NvEnc` from a CPU-side stream.

- M352: `NvDec` becomes multi-domain, advertising `{Cuda, System}` output and resolving via the M351 negotiation.

- M351: adds two-sided allocation-domain negotiation: `DomainSet` lets producer/consumer each advertise multiple acceptable domains, and the tee join picks the best common one instead of failing loud.
- M350: fixation for diamonds becomes a bounded backtracking search over arc-consistency-narrowed domains, fixing greedy per-edge fixation picking a jointly-inconsistent solution.
- M349: validates mid-stream `ElementSlot` hot-swap inside a running graph under load (previously only unit-tested in isolation).
- M348: adds `run_muxer_sink_dynamic`, merging runtime-added inputs' output into a trailing sink for a `muxer ! filesink` shape.
- M347: adds `GraphTemplate`, wrapping a graph-builder closure so a fresh element set can be instantiated per run (replay, retry, A/B).
- M346: adds `FanOutPolicy::AllowBranchDrop`, letting a tee drop just the branch that rejects a mid-stream `CapsChanged` instead of failing the whole run.
- M345: adds a `debug_assert!` guard on `DerivedCoupled` verifying its `passthrough` field mask matches what the `derive` closure actually passes through.
- M344: mid-stream β allocation re-cascade now crosses a muxer: a `CapsChanged` on one pad re-derives and re-cascades only that pad's branch.
- M343: fixes tee allocation join to intersect params (mismatched domains fail loud with `AllocationConflict`); muxers can now state a per-pad allocation demand via `propose_allocation_for_input`.
- M342: `videoconvert` now converts the full high-bit-depth/4:2:2/4:4:4 matrix via a general planar hub (`convert_via_hub`), not just the fast 8-bit pairs.
- M341: `videoscale`/`videocrop`/`videoflip` negotiate and process the full high-bit-depth/4:2:2/4:4:4 planar family via a shared generic per-plane path.
- M340: adds 10/12-bit AV1 encode to `Av1Enc` (rav1e `Context<u16>`), completing the high-bit-depth round trip end to end.
- M339: AV1 decoders (`Dav1dDec`/`Rav1dDec`) emit 4:2:2/4:4:4 and 10/12-bit; `Av1Enc` gains 8-bit 4:2:2/4:4:4 encode.
- M338: adds high-bit-depth and 4:2:2/4:4:4 raw-video `RawVideoFormat` variants (I420p10/12, I422*, I444*) as caps groundwork.
- M337: adds pure-Rust AV1 decode via `re_rav1d` (`Rav1dDec`, `rav1d` feature), the Rust-only counterpart of `Dav1dDec`.
- M336: adds AV1 decode via libdav1d (`Dav1dDec`, `dav1d` feature), `CompressedVideo{Av1}` -> `RawVideo{I420}`.
- M335: adds `CapabilityDescriptor` (memory domain + hardware + rank) for context-aware auto-plug element selection.
- M334: adds FlexFEC (RFC 8627) forward error correction, a wide-mask FEC on a dedicated SSRC covering up to 109 packets per repair.
- M333: adds SRT live-mode congestion control (`LiveCc`), pacing egress to a fixed or input-tracking bandwidth via `SrtSink::with_max_bandwidth`.
- M332: adds SRT mid-stream key rotation (`with_key_rotation`) using the even/odd `KK` slots so in-flight packets keep decrypting across a rekey.
- M331: adds AES-256 support to the SRT stream cipher (`with_aes256()`), alongside the existing AES-128, negotiated via the KM `KLen`.
- M330: adds interleaved (column) ULPFEC, recovering a burst of consecutive losses that single-level FEC cannot.
- M329: adds RTCP sender reports to `UdpSink` (`with_rtcp_sender_reports`), pairing NTP time with RTP timestamp and packet/octet counts.
- M328: adds SRT TSBPD receiver timing (`set_tsbpd`), holding packets to a fixed buffering delay for steady output.
- M327: `g2g-pyapi` now builds a portable abi3 wheel (py>=3.11) with type stubs, plus a new CI `bindings` job.
- M326: adds a redistributable `g2g-pyapi` wheel (maturin) plus in-tree C and Python example programs.
- M325: `g2g-pyapi` `AppSink.pull()`/`try_pull()` now return a zero-copy `FrameView` instead of copying into `bytes`.
- M324: adds progressive (non-fragmented) MP4 demux to `Mp4Src`, alongside the existing fMP4 fragment path.
- M323: runs a pure-g2g file -> hosted-YOLO -> overlay -> display pipeline via `pyelement`; fixes 5 real bugs in the file-decode path.
- M322: hosts the real gst-python-ml `objectdetector` under `GSTML_BACKEND=g2g`, running ONNX YOLO inference on frames through PyO3.
- M321: `g2g-python` forwards declared element properties (model-name, device, etc.) to the hosted Python instance via `setattr`.
- M320: adds fan-in request pads (`run_aggregator_dynamic`'s `DynamicFaninHandle::add_input`), attaching a source to a running aggregator.
- M319: adds dynamic broadcast tee (`run_source_tee_dynamic`), sharing every frame zero-copy to every attached branch.
- M318: probes zero-copy libcamera -> GPU dma-buf import on real hardware; finds it not viable on a USB camera + discrete GPU.
- M317: adds `LibCameraSrc` image controls (brightness/contrast/saturation) and camera selection by id substring.
- M316: adds `LibCameraSrc` exposure/gain controls (`with_exposure`/`with_gain`), the real lever for frame rate in low light.
- M315: adds `libcamera-wgpu` feature chaining `LibCameraSrc -> VideoConvert(NV12) -> WgpuPreprocess` into a GPU f32 NCHW RGB tensor.
- M314: `LibCameraSrc` gains MJPEG capture (`with_mjpeg`/`format=mjpeg`), emitting `CompressedVideo{Mjpeg}` via a new `OutKind` enum and `format` property.
- M313: `LibCameraSrc` pins requested fps on the camera via `FrameDurationLimits`, instead of only stamping PTS.
- M312: adds `LibCameraSrc` (`libcamera` feature, Linux), an NV12/YUYV capture source over the libcamera stack, registered as `libcamerasrc` with camera/width/height/framerate properties.
- M311: adds `OnvifSrc` (`onvif` feature): WS-Discovery + SOAP stream-URI resolution, delegating RTP/decode to `RtspSrc`; `RtspSrc` gains `with_credentials`.
- M310: adds runtime dynamic fan-out branches via `run_source_router_dynamic` / `DynamicFanoutHandle::add_branch`, built on a new no-spawn `DynamicJoin` primitive.
- M308: adds `Camera2Src` (`camera2` feature): NDK Camera2 capture to tight NV12, registered as `camera2src`.
- M307: adds `AAudioSink`/`AAudioSrc` (`aaudio` feature) via `ndk::audio`, registered as `aaudiosink`/`aaudiosrc`.
- M306: adds `MediaCodecEnc` (`mediacodec` feature): NV12 in, Annex-B H.264/H.265 out, registered as `mediacodecenc`/`mediacodecench265`.
- M305: presents the decoded wgpu texture through a `wgpu::Surface` over an Android `ANativeWindow`, closing the decode->GPU->display loop on-GPU.
- M304 (pipelining + negotiation): `YcbcrToRgba` pipelines conversion via a `RING_DEPTH` slot ring instead of blocking per frame, and caps negotiation now advertises both NV12 and RGBA for GPU-output mode.
- M304 (decode -> preprocess on GPU): `WgpuPreprocess` consumes the RGBA `WgpuTexture` from `MediaCodecDec`'s GPU-output mode via a new `TEX_SHADER_RGBA` compute path, closing the decode->preprocess loop on-GPU.
- M304 (complete): `MediaCodecDec::with_gpu_output()` keeps decoded frames on-GPU end to end via a reusable `YcbcrToRgba` converter, emitting `MemoryDomain::WgpuTexture` through a new `WgpuRgbaTexture` keep-alive.
- M304 (ycbcr conversion): `ahb_to_rgba_readback` converts the opaque AHardwareBuffer to RGBA in raw Vulkan via `VkSamplerYcbcrConversion` + a compute shader, since wgpu can't express the immutable sampler.
- M304 (first step): adds the `mediacodec_wgpu` bridge module + `create_android_interop_device` / `ahb_format_info`, and a capture hook on `MediaCodecDec` to probe a real decoded buffer's format.
- M303: adds H.265/HEVC to `MediaCodecDec` (registered `mediacodecdech265`), with codec-specific data packing VPS+SPS+PPS into `csd-0`.
- M302: `MediaCodecDec` reworks output onto an `AImageReader` Surface (vendor decoders only deliver gralloc buffers), packing `YUV_420_888` planes to NV12; registered as `mediacodecdec`.
- M301: adds a `linux-full` cargo feature bundling all Linux-desktop elements, plus `cargo xtask install-launch` to auto-detect host deps and install `g2g-launch` with the supported feature set.
- M300: adds VP8/VP9 video to A/V `mp4mux` (`Mp4MuxN`) via `vp09`/`vp08` sample entries + `vpcC` boxes; VP parsers moved to shared `fmp4mux`.
- M299: adds runner-level PTS-ordered fan-in: `MultiInputElement::input_pts_ordered()` opts into `muxer_arm_pts`, delivering frames via a runner-owned `InputAggregator` in global PTS order.
- M298: adds Opus audio to A/V `mp4mux` (`Mp4MuxN`) via an `Opus` sample entry + RFC 8316 `dOps` box.
- M297: adds VP8/VP9 video + Opus audio to A/V `matroskamux` (`MkvMuxN`), producing WebM with raw VP frames and a synthesised `OpusHead` `CodecPrivate`.
- M296: adds A/V `flvmux` (`FlvMuxN`): H.264+AAC fan-in muxer emitting FLV AVC/AAC sequence headers plus interleaved media tags by PTS.
- M295: removes the `Mp4Sink` convenience element; `mp4sink.rs` renamed `fmp4mux.rs`, keeping the muxer box-writer reused by `mp4mux`/`mp4muxn`/`mkvmuxn`.
- M294: adds A/V `matroskamux` (`MkvMuxN`): H.264/H.265 + AAC multi-track muxer, access units interleaved by PTS into shared Clusters.
- M293: adds A/V `mp4mux` (`Mp4MuxN`): H.264/H.265 + AAC fragmented-MP4 muxer with one `trak` per stream, interleaved by PTS.
- M292: adds `avenc_aac` (`aacenc`), a libavcodec AAC-LC encoder: PCM in, ADTS-framed AAC out.
- M291: adds `mp4mux`: H.264/H.265 Annex-B in, ISO-BMFF (`ftyp`+`moov` then `moof`+`mdat` per AU) out; extracted `Fmp4Muxer` shared by `Mp4Sink`/`Mp4Mux`.
- M290: `VideoRate::auto()` (new default) takes its target framerate from negotiated output caps via a `DerivedCoupled` constraint, instead of a fixed 30fps default.
- M289: fixes `x264enc` (`FfmpegH264Enc`) failing to open by allocating the context with `Context::new_with_codec` instead of generic defaults.
- M288: fixes stacked auto-converters (`videoconvert ! videoconvert`) mislabeling output; converters now forward the arg downstream and track `last_caps` instead of overwriting their input.
- M287: adds `RunStats::report()`: frame counts, drop rate, declared latency window, elected clock, negotiated allocation; `g2g-launch` prints it at end.
- M286: adds a `runner` criterion bench (bounded per-edge channel throughput) plus a `bench.yml` CI job that fails on >50% mean regression vs base.
- M285: adds `output_memory` to `AsyncElement`/`SourceLoop`; `negotiate_graph` reports per-edge memory domain so `g2g-launch --dot` bolds GPU/zero-copy links.
- M284: adds a standalone `g2g-bench` crate with `caps` and `convert` criterion benches, plus `cargo xtask bench`.
- M283: adds `xtask ffi-probe <header> <struct> [--field f]...`, generating a C probe for struct size/offsets and a ready-to-paste size assert.
- M282: adds `negotiate_graph` (caps solve without running the pipeline); `g2g-launch --dot` renders fixated caps per edge, falling back to topology-only on failure.
- M281: adds an `xtask` dev-command crate (`cargo xtask <cmd>`) with `ci`, `test --here`, `size`, and `wasm` subcommands.
- M280: `solve_graph` narrates DAG negotiation under a `caps` log category (setup + per-edge caps, or a failure explanation), enabled via `G2G_CAPS_TRACE`/`G2G_DEBUG=caps`.
- M279: adds `Graph::to_dot`/`ValidatedGraph::to_dot` (role-coded Graphviz DOT dump with caps/memory/policy annotations), wired into `g2g-launch --dot`.
- M278: rewires `examples/bevy-g2g-stream` to render on g2g's interop wgpu device and encode via native `NvEnc` through `WgpuToCuda`, with no GPU->CPU read-back.
- M277: `NvEnc` reuses a single output-bitstream buffer across frames instead of per-frame alloc, and supports runtime bitrate retarget via `nvEncReconfigureEncoder`.
- M276: adds `ElementDesc::output_memory` memory-feature metadata; auto-plug biases ties toward a chain matching a requested memory domain (e.g. `nvdec` for `Cuda`).
- M275: `WgpuToCuda` becomes a pipeline `AsyncElement` with a `LinearBufferPool` reuse pool, replacing per-frame `cuMemAlloc`/`cuMemFree`.
- M274: registers the ffmpeg H.264 encoder as `ffmpegenc`/`x264enc`, plus a software-first `avenc_h264` alias.
- M273: adds HEVC (H.265) support to native `NvEnc`/`NvDec` via a `codec` field/property and codec-inferred NVCUVID setup.

- M272: registers `NvEnc`/`NvDec` as `nvenc`/`nvdec` with GStreamer-canonical aliases; `NvDec` registered last as an auto-plug candidate to avoid hijacking CPU pipelines.

- M271: adds `WgpuToCuda` (`cuda-wgpu` feature), a zero-copy wgpu->CUDA bridge, plus RGBA input for `NvEnc`, for a no-PCIe-download GPU-render encode path.

- M270: adds `NvDec` (`nvdec` feature): native NVDEC H.264 decode via NVCUVID parser/decoder, emitting NV12 in CUDA device memory.

- M269: adds `NvEnc` (`nvenc` feature): native NVENC H.264 encode ingesting a CUDA NV12 surface in place via hand-rolled `nvEncodeAPI` FFI.

- M267: adds `examples/bevy-g2g-stream`: headless Bevy render joined to g2g via `GpuContext::from_wgpu`, read back and encoded through `FfmpegH264Enc(NVENC)` to file or WHIP.

- M268: fixes the Cortex-M build of `g2g-core` (switches `clock.rs` to `portable_atomic::AtomicU64`) and adds a `g2g-size` footprint-measurement harness (~3.2KB `.text`).

- M266: adds `FfmpegH264Enc` (`ffmpeg` feature, Linux): H.264 encoder via `ffmpeg-next` with `Nvenc`/`Software` backends, in-band SPS/PPS, PLI-forced IDR.

- M265: adds `relu`/`sigmoid` activations and `maxpool2d`/`avgpool2d` pooling as GPU compute passes in `WgpuInference`, with a weightless 3-entry bind-group path.

- M264: runs a pipeline as independently spawned tasks on a real `embassy-executor` via a new `Sync` `SharedPacketChannel`, instead of one joined future.

- M263: adds `GpuContext::from_wgpu(instance, adapter, device, queue)`, wrapping an embedder's existing wgpu device so GPU elements produce textures directly usable in its render graph.

- M262: adds a dependency-free `g2g-ml::safetensors` reader/writer and `WgpuInference::conv2d_from_safetensors` to import trained weights at runtime.

- M261: adds GPU-resident 2D convolution to `WgpuInference` (`conv2d`), sharing the linear op's device-adopt/dispatch/read-back machinery via a new `CONV_SHADER`.

- M260: adds `StaticLendRing`, a no-alloc zero-copy DMA capture ring with per-slot lease-based backpressure, proving a heap-free capture path end to end.

- M259: `DerivedOutput` re-derives on mid-stream input change via `caps::project_passthrough_derived`; also fixes `discover_passthrough` unsoundness on multi-valued closures.
- M258: mid-stream backward-feasibility snapshot now couples through a `DerivedOutput`'s discovered passthrough mask via `couple_passthrough_derived`, instead of returning `None`.
- M257: adds `caps::discover_passthrough` + `solver::derived_backward`, narrowing a `DerivedOutput`'s input field-by-field instead of only dropping input alternatives.
- M256: adds `PoolStage`, a passthrough transform that rebuilds its `BufferPool` on the mid-stream β allocation re-cascade.
- M255: adds `CudaKmsSink` (`cuda-kms` feature): CUDA-GL display on DRM/KMS via GBM + page-flips, sharing NV12 render state with `CudaGlSink` via a new `glnv12` module.
- M254: adds `CudaWgpuPool`, recycling `CudaToWgpu`'s exportable image/CUDA-import/texture instead of allocating per frame; ~2.6x faster bridge step at 1080p.
- M253: adds a `cudagl_vs_wayland` A/B present-latency benchmark; device-resident `CudaGlSink` is 10.7x faster than `WaylandSink` at 1080p.
- M252: `CudaGlSink` compiles and validates on-display on the RTX 3060; NV12 CUDA->GL interop, presents 60 frames at ~8ms p50 latency.
- M251: CUDA<->wgpu keep-on-GPU zero-copy chain (`NvdecCuda`->`CudaToWgpu`->`WgpuPreprocess`->`WgpuInference`) validated on RTX 3060, matches CPU reference with no PCIe download.
- M250: tee broadcast is zero-copy via `MemoryDomain::share`/`make_shareable` and new `SystemSliceInner::Shared`/`SharedPooled`, with copy-on-write on mutation.
- M249: bidirectional WebRTC on one PeerConnection via `MultiDuplexSession`/`DuplexInbound` and `run_duplex_session`; new `WebRtcDuplexSession` element.
- M248: multi-track A/V WebRTC session validated against mediamtx; `WebRtcSessionSink`/`WebRtcWhepSessionSrc` publish/subscribe H.264+Opus on one connection.
- M247: WebRTC WHIP/WHEP validated against mediamtx; fixed `WebRtcWhepSrc`/`WebRtcWhepSessionSrc` caps fixation (`Dim::Any`/`Rate::Any` -> `Range`).
- M246: multi-track WebRTC ingress via new `MultiOutputSource` trait + `run_fanout_session` runner; `WebRtcWhepSessionSrc` demuxes H.264 + Opus by `Mid`.
- M245: multi-track WebRTC egress via new `run_fanin_session` runner + `MultiInputElement`; `WebRtcSessionSink` carries H.264 video and Opus audio over one WHIP session.
- M244: WebRTC adaptive bitrate via `PushOutcome::Bitrate`/`take_bitrate`; `WebRtcSink` uses str0m BWE, `Av1Enc` retargets rav1e bitrate with 20% hysteresis.
- M243: WebRTC keyframe-request (PLI) via `Reconfigure::ForceKeyframe`/`take_reconfigure`; `WebRtcSink` relays remote PLI upstream, `WebRtcWhepSrc` requests keyframes on join.
- M242: WebRTC TURN relay client (`turn.rs`, RFC 5766/8656) for `WebRtcSink`/`WebRtcWhepSrc`; `WebRtcWhepSrc` also gains Opus ingest.
- M241: WebRTC ICE STUN candidate gathering (`with_stun_server`) on `WebRtcSink`/`WebRtcWhepSrc` for NAT traversal.
- M240b: g2g<->g2g WHIP->WHEP loopback smoke test (`webrtc_whip_to_whep_loopback`).
- M240: native WebRTC ingest (`WebRtcWhepSrc`, `webrtc` feature), the receive-side inverse of `WebRtcSink`; video-only via str0m.
- M239: `WebRtcSink` gains Opus audio track selection; adds `whep-player.html` and a WHIP smoke test.
- M238: native WebRTC egress (`WebRtcSink`, `webrtc` feature) publishes H.264 to a WHIP server over str0m (ICE/DTLS/SRTP).
- M237: ffmpeg VAAPI hwaccel decode (`FfmpegVideoDec` `Backend::Vaapi`); hardware-validated on AMD Rembrandt 680M, registered as `ffmpegvaapidec`.
- M236: Python bindings (`g2g-pyapi`, PyO3): `g2g.Pipeline`/`AppSrc`/`AppSink` drive whole pipelines from Python.
- M235: `appsink` pull mode + zero-copy held samples via `g2g_appsink_pull`/`g2g_appsink_try_pull` and `G2gSample`.
- M234: zero-copy `appsrc` lend via `SystemSlice::from_foreign` and `g2g_appsrc_push_lend`; mutating consumers copy-on-write.
- M233: `appsrc`/`appsink` elements + C API (`g2g_appsrc_new`/`push`/`end_of_stream`/`free`, `g2g_appsink_set_callback`); v1 copies bytes.
- M232: C ABI (`g2g-capi`, cdylib/staticlib + `g2g.h`): `g2g_pipeline_launch`/`bus_poll`/`wait`/`free` over the launch DSL.
- refactor: shared encoder frame-emit loop (`encoder_base::emit_packets`) and shared `pixel::is_yuv420`/`frame_byte_size` helpers.
- M231: macOS VideoToolbox H.264 encode (`VtEncode`, `vtencode` feature), NV12 -> Annex-B via `VTCompressionSession`.
- M230: multi-client RTSP server (`rtspserversink`), per-client handshake and RTP session, broadcasts to all connected players.
- M229: SRT encryption (`srtsink`/`srtsrc` `with_passphrase`): AES-128-CTR, PBKDF2-HMAC-SHA1 KEK, RFC 3394 key wrap.
- M228: RTSP server source (`rtspserversrc`) accepts a publisher (ANNOUNCE/SETUP/RECORD) and depayloads RTP H.264.
- M227: field-level bidirectional caps coupling (`DerivedCoupled` + passthrough mask); resolves the `scale_then_convert` limit.
- M226: per-frame keyframe flag + trick-mode KEY_UNIT playback (seek depth)
- M225: RTP forward error correction (ULPFEC, RFC 5109)
- M224: SRT transport (`srtsink` / `srtsrc`) - reliable low-latency contribution.
- M223: RTSP server (`rtspserversink`) - host an RTSP endpoint.
- M222: RFC 4588 RTP retransmission (RTX)
- M221: RTMP egress (`rtmpsink`) - the publish side of RTMP.
- M220 (Stage 1): CUDA->wgpu zero-copy bridge (`CudaToWgpu`) - GPU keep-on-GPU pillar complete.
- M220 (Stage 0): CUDA<->wgpu keep-on-GPU correctness scaffold.
- M219: Android platform track begins -- NDK MediaCodec H.264 decode (`MediaCodecDec`)
- M218: macOS platform track begins -- VideoToolbox H.264 decode (`VtDecode`)
- M217: input-side GPU surface-import for `WgpuPreprocess`.
- M216: GPU-resident tensor inference consumer (`WgpuInference`)
- M215: GPU-resident tensor output for `WgpuPreprocess`.
- M214: zero-copy GPU fan-out validated on a real GPU.
- M213: zero-copy GPU frame fan-out.
- M212: reverse playback at the sink.
- M211: non-flushing (accumulating) seek.
- M210: demux node in `run_graph` + `gst-launch` fan-out wiring.
- M209: flattening bins + ghost pads.
- M208: `gst-launch` fan-in for the multi-stream TS muxer.
- M207: multi-stream MPEG-TS muxing (A+V into one TS)
- M206: bus breadth (`StreamStart`, `Info`)
- M205: content-based demultiplexer (multi-output fan-out)
- M204: PTS-ordered muxer fan-in.
- M203: application query system (position + duration)
- M202: `tracing` bridge for the logging facade.
- M201: dynamic plugin loading via cargo (`dlopen` a `cdylib`)
- M200: GStreamer porting story (guide + infra)
- M199: `InputAggregator`, a shared per-input collector for muxers.
- M198 (skeleton): host gst-python-ml elements as first-class g2g elements.
- M197 (coverage): branching decode pipelines.
- M196: `uridecodebin` / `playbin` in gst-launch lines.
- M195: reflect `bytestream-format` into decodebin's auto-plug input.
- M194: container-demux auto-plug.
- M193: `decodebin` in gst-launch lines (auto-plug a decode chain)
- M192: gst-canonical-name element aliases.
- M191: `g2g-launch` CLI flag parity with gst-launch.
- M190: `queue` in gst-launch lines (maps to per-edge LinkPolicy)
- M189: mid-stream re-cascade re-resolves caps-driven transform output.
- M188: forward-resolve walk (stacked auto transforms)
- M187: audio sample-rate "any" + caps-driven audioresample.
- M186: caps-driven videoconvert (format from the solve)
- M185: caps-driven transforms (videoscale takes its target from the solve)
- M184: format-less / partial geometry caps.
- M183: align videocrop / videobox property model with GStreamer.
- M182: harmonize the gst-launch DSL vocabulary with GStreamer.
- M180: strided `TensorView` substrate + zero-copy `VideoFlip`.
- M179: element-granular logging framework (`GST_DEBUG` analog) + instance names.
- M178: rich `gst-inspect` introspection (element metadata + detailed properties)
- M177: Opus audio encode + decode (`OpusEnc` / `OpusDec`, `opus` feature)
- M176: presentation base time anchored at the `Playing` transition.
- M175: relay QoS through a transform (multi-element pipelines shed at the source)
- M174: upstream QoS propagation, a source sheds load when its sink falls behind.
- M173: QoS late-drop in `WaylandSink`.
- M172: PTS pacing in the DAG runner, `run_graph` delivers `ClockSync`.
- M171: subtitle overlay (`textoverlay`) with SRT and WebVTT.
- M170: `g2g-launch` / `g2g-inspect` command-line binaries.
- M169: clock-synchronised presentation, frames present at their PTS.
- M168: Linux audio sinks and capture via alsa/pulse/pipewire, plus mfvideosrc.
- M167: RTMP ingest via `RtmpSrc` and a sans-IO RTMP session.
- M166: DASH live dynamic MPD reload.
- M165: DASH SegmentTimeline + $Time$ addressing.
- M164: fMP4 cbcs Common Encryption decryption in Fmp4Demux.
- M163: HLS SAMPLE-AES key auto-wiring, `HlsSrc` to `SampleAesDecrypt`.
- M162: HLS SAMPLE-AES sample decryptor (TS H.264 + AAC)
- M161: HLS AES-128 segment decryption (EXT-X-KEY)
- M160: DASH (MPD parser + DashSrc, SegmentTemplate)
- M159: fMP4 / CMAF over HLS (EXT-X-MAP wiring)
- M158: Fmp4Demux (fragmented-MP4 / CMAF byte-stream demux)
- M157: live HLS playlist reload.
- M156: HLS playlist parser + HlsSrc (adaptive streaming, VOD)
- M155: HttpSrc (HTTP(S) byte-stream source)
- M154: I420 in/out for the MJPEG codecs.
- M153: MjpegEnc (Motion-JPEG encode, pure Rust)
- M152: MjpegDec (Motion-JPEG decode, pure Rust)
- M151: VpxEnc (VP8 / VP9 encode via libvpx FFI)
- M150: Av1Enc (pure-Rust AV1 software encode)
- M149: accurate seek, segment clipping at the sink.
- M148: Mp4Src seek-aware for scrub / edit repositioning.
- M147: mp4sink writes udta/ilst tags.
- M146: compositor configurable background.
- M145: matroskamux Cluster batching.
- M144: matroskademux DefaultDuration frame timing.
- M143: matroskademux unknown-size Clusters for live WebM.
- M142: flvmux writes onMetaData.
- M141: matroskamux writes Tags.
- M140: mp4src udta/ilst tags.
- M139: matroskademux Tags + Segment Title.
- M138: flvdemux onMetaData tags (AMF0)
- M137: tag system for stream metadata on the bus.
- M136: av1parse (AV1 sequence-header parser)
- M135: vp9parse (VP9 uncompressed-header parser)
- M134: vp8parse (VP8 keyframe parser)
- M133: opusparse (Opus TOC parser)
- M132: V4l2Src + CudaGlSink native caps_constraint.
- M131: UdpSrc + RtspSrc native caps_constraint.
- M130: audiomixer (summing fan-in muxer)
- M129: videobox (borders / letterbox)
- M128: audiopanorama (stereo pan)
- M127: alpha (alpha control / chroma key)
- M126: volume (audio gain)
- M125: AudioTestSrc waveform coverage.
- M124: videobalance (brightness / contrast / saturation)
- M123: VideoTestSrc pattern coverage.
- M122: gst-launch muxer fan-in.
- M121: fix double-EOS forward in demux/mux/overlay transforms.
- M120: FLV muxer (`flvmux`)
- M119: FLV demuxer (`flvdemux`)
- M118: gst-launch branching (`tee` / named pads)
- M117: gst-launch caps-filter syntax (Caps text grammar)
- M116: Ogg / Opus demuxer (`oggdemux`)
- M115: Matroska / WebM muxer (`matroskamux`)
- M114: MPEG-TS muxer (`mpegtsmux`)
- M113: Matroska block lacing (Xiph / EBML / fixed)
- M112: filesrc byte-stream caps + content typefind.
- M111: multi-codec ffmpeg video decoder (VP8 / VP9 / AV1 / H.265)
- M110: Matroska / WebM demuxer (`matroskademux`)
- M109: TsDemux audio (AAC) + H.265 stream selection.
- M108: MPEG-TS demuxer + `Caps::ByteStream`.
- M107: property coverage + `default_registry` for out-of-the-box gst-launch use.
- M106: `gst-launch` text pipeline parser.
- M105: registry build-by-name + `gst-inspect` dump.
- M104: runtime property system.
- M103: `WgpuSink` GPU presentation sink.
- M102: Vello GPU analytics overlay + `MemoryDomain::WgpuTexture`.
- M101: analytics overlay element (CPU)
- M100: analytics metadata through fan-out (Arc/COW)
- M98-M99: per-frame metadata system and detection post-processing.
- M97: compositor per-pad scaling.
- M96: RTCP feedback loop and NACK-based retransmission.
- M94: RTP receive-side jitter buffer for raw RTP ingest.
- M93: `Compositor`, a software RGBA8 video mixer.
- M92: `uridecodebin` URI front door to the autoplug registry.
- M91: `UdpSrc` UDP/RTP H.264 ingress source.
- M90: `V4l2Src` Linux V4L2 capture source.
- M89: `RawVideoFormat::Yuyv` packed 4:2:2 + videoconvert unpacking.
- M88: reserve the `FrameMeta` extension point on `Frame`.
- M87: `Buffering` bus messages from link occupancy.
- M86: leaky `LinkPolicy` queue with drop observability.
- M85: QoS bus messages + late-frame dropping in `SyncSink`.
- M84: `AudioResample` audio transform.
- M83c: source registry + `playbin`-style assembly + VAAPI decoder templates.
- M83b: caps-aware factories + `decodebin` splice + decoder templates.
- M83a: auto-plug search + element registry for `decodebin`.
- M82: end-to-end flush seek via `SeekController` + seek-aware source.
- M81: runner emits opening SEGMENT + error-priority fix.
- M80: `PipelinePacket::Segment` carrier.
- M79: seek + segment model with running-time / stream-time math.
- M78: state machine + preroll over the DAG runner.
- M77: preroll + live/non-live state changes.
- M76: pipeline state machine (NULL/READY/PAUSED/PLAYING).
- M75: `AacParse` ADTS header parser.
- M74: `run_linear_chain` as thin builder + topology-derived rejection policy.
- M73: legacy-bridge support in `solve_graph` + `run_muxer_sink` thin builder.
- M72: lifetime-generic graph runner for borrowed graphs.
- M71: DAG runner stat folds + muxer MX-1/MX-2 in `run_graph`.
- M70: DAG runner mid-stream re-solve + beta allocation re-cascade.
- M69: DAG runner fan-in muxer nodes in `run_graph`.
- M68: `H265Parse` HEVC SPS parser.
- M67: DAG runner `run_graph`.
- M66: `VideoFlip` software flip / rotate.
- M65: DAG runner muxer fan-in via `NodeConstraint`.
- M64: DAG runner `solve_graph` topological CSP.
- M63: DAG runner `Graph` builder + validation.
- M62: `VideoCrop` software rectangular crop.
- M61: native end-to-end ML pipeline test.
- M59: `OrtInference` tensor-input mode.
- M58: `WebCodecsDecode` GPU-resident output.
- M57: `WebGPUExternalTexture` memory domain + `WebGPUKeepAlive`.
- M56: `VideoRate` software temporal resampler.
- M55: `VideoScale` software spatial resampler.
- M54: `FfmpegH264Dec` accepts YUV444P source, chroma downsampled to 4:2:0.
- M53: CUDA execution provider for `OrtInference`.
- M52: `H264Parse` AVCC framing + VUI framerate.
- M51: pure-Rust Burn inference backend, `BurnInference`.
- M50: inline GPU tensor preprocessing, `WgpuPreprocess`.
- M47: UDP egress sink, `UdpSink`.
- M46: sans-IO H.264 RTP packetizer, `RtpH264Packetizer`.
- M45: embassy-sync zero-alloc packet link, `PacketChannel`.
- M44: Cortex-M readiness, `portable-atomic` for the metrics histogram.
- M43: Embedded/Embassy foundation, `StaticBufferPool` + `EmbassyClock`.
- M42: WebRTC data-channel ingest, `WebRtcSrc`.
- M41: in-browser presentation, `CanvasSink`.
- M40: WebCodecs decode, `WebCodecsDecode`.
- M39: Browser/Wasm foundation, `WasmClock` + `WebSocketSrc`.
- M38: WASAPI loopback capture in `WasapiSrc`.
- M37: AAC audio in fMP4 container, `Mp4AudioSink` / `Mp4AudioSrc`.
- M36: `MfAacDecode` AAC audio decoder.
- M35: `MfAacEncode` AAC audio encoder.
- M34: `AudioConvert` PCM converter.
- M33: async-MFT support in `MfEncode` for hardware encoders.
- M32: `WasapiSrc` WASAPI capture source.
- M31: HEVC in fMP4 container, `Mp4Sink` / `Mp4Src`.
- M30: HEVC support in the MF encode/decode elements.
- M29: `WasapiSink` WASAPI render sink.
- M28: `Mp4Src` fragmented-MP4 demuxer source.
- M27: `TensorPostprocess` classification head.
- M26: DirectML execution provider for `OrtInference`.
- M25: first audio elements, `AudioTestSrc` / `WavSink`.
- M24: `Mp4Sink` fragmented-MP4 muxer sink.
- M23: `VideoConvert` software raw-format converter.
- M22: `TensorBatcher` bootstraps `g2g-enterprise`; per-input Eos contract.
- M21: first `g2g-ml` element, `OrtInference` (ONNX Runtime).
- M20: file I/O elements, `FileSink` / `FileSrc`.
- M19: `MfEncode` Windows H.264 encode element.
- M18 item 4 follow-up: dyn interior elements contribute clock + latency.
- M18 item 4 follow-up: caps-alpha mid-stream re-solve over downstream subgraph.
- M18 item 4 follow-up: beta allocation re-cascade over N hops.
- M18 item 7 (complete): bus wiring across every runner.
- M18 item 4: arbitrary-length linear runner, `run_linear_chain`.
- M18 item 7: structured negotiation failures on the bus.
- M18 item 1 (Session E): beta allocation re-cascade, single hop.
- M16 step 5: migrate the display sinks to native `caps_constraint`.
- M16 step 6 audit: CapsFilter + ACCEPT_CAPS confirmed fully wired.
- M12: allocation query runs before `configure_pipeline`.
- M18 item 6: pad templates for the Windows decode/display elements.
- W1: allocation-query handshake for the D3D11 path.
- W1 (Phase 4): `D3D11Sink` present sink.
- W1 (Phase 3): `MfDecode` zero-copy D3D11 texture output.
- W1 (Phase 2): `MfDecode` DXVA / D3D11 hardware decode.
- W1 (Phase 1): Direct3D 11 memory domain foundation.
- M13: `MfDecode` NV12 stride handling.
- C3 (Phase 3, step 3): allocation-query handshake for the CUDA path.
- C3 (Phase 3, step 2b): `CudaGlSink` (first draft, owed first compile)
- C3 (Phase 3, step 2a): CUDA-GL interop foundation.
- C3 (Phase 3, step 1): `CudaDownload` device->host bring-up element.
- C3 (Phase 2): NVDEC CUDA hwframe output (`Backend::NvdecCuda`)
- C3 (Phase 1): CUDA memory domain foundation.
- LatencyProfile knob on runners.
- RtspSrc: stash post-SETUP session, skip duplicate connect.
- M17: split `VideoFormat` into codec vs. raw pixel layout.
- M18 item 5: async `SourceLoop::intercept_caps`.
- caps-nego: drop per-frame `Frame.caps`.
- NVDEC backend low-latency defaults + `wayland_smoke` knobs.
- NVDEC backend for `FfmpegH264Dec` (NVIDIA hardware H.264 decode)
- M18: GStreamer parity push, item-by-item.
- M16: Caps negotiation redesign (CSP framing)
- M12: Live-source surface (latency + allocation + clock election)
- M15: RTSP reconnect + long-running stability soak.
- M14: Wayland display sink (Linux, NV12, desktop-dev convenience)
- M14: KMS/DRM display sink (Linux, NV12)
- M13: End-to-end RTSP → ffmpeg decode (Linux software path)
- M13: Windows hardware decode (Media Foundation)
- M11: Application control surface.
- M10: True muxer fan-in.
- M9: Dynamic fan-out / fan-in.
- M8: Caps negotiation + dynamic element swap.

## M0-M5 (prior)
- M0/M1: Cargo workspace scaffold and minimal `no_std + alloc` pipeline runtime.
- M2: async `OutputSink`, identity transform, 3-element runner.
- M3: `AsyncClock` + `WallClock` + `SyncSink` for PTS-paced presentation.
- M4: Arc-recycled `BufferPool` with async `acquire`; DMABUF close-on-drop fix.
- M5: `RtspSrc` element wrapping `retina`.

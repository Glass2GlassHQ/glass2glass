# g2g-plugin

SDK for building dynamically loadable
[glass2glass](https://github.com/boxerab/glass2glass) plugins: the
`declare_plugin!` macro and the ABI tag a host checks before `dlopen`ing a
third-party element `cdylib`.
